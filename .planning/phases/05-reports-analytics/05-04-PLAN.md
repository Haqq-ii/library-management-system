---
phase: 05-reports-analytics
plan: 04
type: execute
wave: 4
depends_on: ["05-01", "05-03"]
files_modified:
  - src/features/reports/activity.ts
  - src/features/reports/BorrowingActivityChart.tsx
  - src/app/(app)/reports/page.tsx
  - tests/unit/report-activity.test.ts
autonomous: true
requirements: [RPT-03]
must_haves:
  truths:
    - "Librarian opens the Borrowing Activity tab and sees an interactive line chart"
    - "The chart plots loans issued and loans returned per day over the selected range"
    - "Two labeled series are visible: 'Loans Issued' and 'Loans Returned'"
    - "Default date range is the last 30 days; From/To + Apply Filter refetch the chart"
    - "When the range has no loans or returns, the tab shows 'No activity data'"
  artifacts:
    - path: "src/features/reports/activity.ts"
      provides: "getBorrowingActivity Server Action grouping loans/returns by day"
      exports: ["getBorrowingActivity"]
      contains: "requireRole(\"LIBRARIAN\")"
    - path: "src/features/reports/BorrowingActivityChart.tsx"
      provides: "Recharts LineChart client component with date filter"
      contains: "use client"
    - path: "tests/unit/report-activity.test.ts"
      provides: "Unit tests for auth, day-grouping of issued vs returned, date filter"
  key_links:
    - from: "src/app/(app)/reports/page.tsx"
      to: "src/features/reports/activity.ts"
      via: "getBorrowingActivity initial call, result passed to BorrowingActivityChart"
      pattern: "getBorrowingActivity"
    - from: "src/features/reports/BorrowingActivityChart.tsx"
      to: "recharts"
      via: "ResponsiveContainer + LineChart import"
      pattern: "recharts"
---

<objective>
Deliver RPT-03: the Borrowing Activity chart. Add a `getBorrowingActivity` Server Action that buckets loans issued and loans returned by day over a date range (default last 30 days); add a client `BorrowingActivityChart` using recharts (ResponsiveContainer + LineChart, two series) with From/To filter; wire it into the Borrowing Activity tab of /reports. This is the final report slice and completes the phase.

Purpose: After this plan a librarian sees circulation trends over time as an interactive chart — the last of the four required report types.
Output: getBorrowingActivity action, BorrowingActivityChart component, wired Borrowing Activity tab, unit tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-reports-analytics/05-UI-SPEC.md
@.planning/phases/05-reports-analytics/05-01-SUMMARY.md
@.planning/phases/05-reports-analytics/05-03-SUMMARY.md
@src/features/audit/actions.ts
@src/features/audit/AuditTable.tsx
@src/features/reports/PopularBooksTable.tsx
@src/app/(app)/reports/page.tsx
@src/components/ui/card.tsx
@prisma/schema.prisma
@tests/unit/fine-actions.test.ts
</context>

<artifacts_produced>
Symbols this plan creates:
- `getBorrowingActivity(params)` — Server Action in `src/features/reports/activity.ts`, params `{ fromDate?: string; toDate?: string }`, returns `ActionResult<ActivityPoint[]>`
- `ActivityPoint` — exported type `{ date: string; loanCount: number; returnCount: number }`
- `BorrowingActivityChart` — client component in `src/features/reports/BorrowingActivityChart.tsx`
- Modifies `src/app/(app)/reports/page.tsx`: replaces the "activity" TabsContent placeholder with `<BorrowingActivityChart initialData={...} initialFrom={...} initialTo={...} />`
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getBorrowingActivity Server Action with unit tests</name>
  <files>src/features/reports/activity.ts, tests/unit/report-activity.test.ts</files>
  <read_first>
    - src/features/audit/actions.ts (ActionResult, "use server", requireRole try/catch, safe date parsing with isNaN guard + end-of-day setUTCHours)
    - src/features/reports/popular.ts (default-30-day window computation pattern from Plan 03 — replicate the same safe-parse + default approach)
    - prisma/schema.prisma (Loan model: issuedAt Timestamptz for "issued"; returnedAt Timestamptz nullable for "returned")
    - tests/unit/fine-actions.test.ts (mock + assertion style)
  </read_first>
  <behavior>
    - Test 1: requireRole rejects → { success:false, error:"FORBIDDEN" }, no DB query.
    - Test 2: LIBRARIAN, default range → returns one ActivityPoint per day in the window (continuous days, gaps filled with zeros), each with date (YYYY-MM-DD string), loanCount, returnCount.
    - Test 3: a loan issued on day X increments that day's loanCount; a loan returned on day Y increments that day's returnCount; issued and returned are counted independently (a single loan can contribute to both an issued-day and a returned-day).
    - Test 4: invalid fromDate/toDate strings fall back to defaults (no NaN dates passed to Prisma).
    - Test 5: dates in output points are strings in YYYY-MM-DD form (UTC), not Date objects.
  </behavior>
  <action>
    Create src/features/reports/activity.ts with "use server". Import requireRole, prisma, ActionResult. Export type `ActivityPoint = { date: string; loanCount: number; returnCount: number }`.
    Implement `getBorrowingActivity(params: { fromDate?: string; toDate?: string }): Promise<ActionResult<ActivityPoint[]>>`:
    - requireRole("LIBRARIAN") in try/catch → FORBIDDEN.
    - Compute [from, to] window using the SAME safe-parse + default-30-days logic as popular.ts (new Date + isNaN guard; default from = 30 days before now, default to = now; end-of-day for to).
    - Query issued loans: prisma.loan.findMany where { issuedAt: { gte: from, lte: to } } select { issuedAt: true }. Query returned loans: prisma.loan.findMany where { returnedAt: { gte: from, lte: to } } select { returnedAt: true } (returnedAt not null implied by the range bounds). Run both in Promise.all.
    - Build a map of YYYY-MM-DD (UTC, via toISOString().slice(0,10)) → { loanCount, returnCount }. Increment loanCount per issued.issuedAt day, returnCount per returned.returnedAt day.
    - Generate a continuous ordered array of ActivityPoint from `from` day to `to` day inclusive (one per UTC day), filling missing days with zeros so the chart x-axis is continuous.
    - try/catch DB → console.error("[getBorrowingActivity]", err) + { success:false, error:"DB_ERROR" }.
    Create tests/unit/report-activity.test.ts (fine-actions.test.ts style): mock requireRole and prisma.loan.findMany (resolve different arrays for the two calls — use mockResolvedValueOnce twice). Cover the five behavior cases.
  </action>
  <verify>
    <automated>cd "C:/Users/ASUS/library-management-system" && npx vitest run tests/unit/report-activity.test.ts</automated>
  </verify>
  <done>All report-activity.test.ts tests pass; getBorrowingActivity enforces LIBRARIAN, returns continuous zero-filled daily points with independent loanCount/returnCount, safe date parsing, and YYYY-MM-DD string dates.</done>
</task>

<task type="auto">
  <name>Task 2: BorrowingActivityChart recharts component and tab wiring</name>
  <files>src/features/reports/BorrowingActivityChart.tsx, src/app/(app)/reports/page.tsx</files>
  <read_first>
    - .planning/phases/05-reports-analytics/05-UI-SPEC.md (RPT-03 Borrowing Activity Tab: ResponsiveContainer width="100%" height={320} wrapping LineChart; two series loanCount + returnCount; X-axis date tick fontSize 12, show every 7th label if range > 14 days; Y-axis allowDecimals={false} tick fontSize 12; Tooltip contentStyle fontSize 12px; chart inside Card with CardHeader CardTitle "Borrowing Activity"; "use client" required; legend "Loans Issued" / "Loans Returned"; chart colors var(--chart-1) for issued, var(--chart-2) for returned; figure + figcaption sr-only "Borrowing activity chart showing loans issued and returned over time"; Skeleton h-[320px] while pending; empty state "No activity data" / "No loans or returns recorded in the selected date range. Try a wider range.")
    - src/features/reports/PopularBooksTable.tsx (date-range filter bar + useTransition refetch pattern to replicate for the chart filter)
    - src/components/ui/card.tsx (Card/CardHeader/CardTitle/CardContent)
    - src/components/ui/skeleton.tsx (Skeleton)
    - src/app/(app)/reports/page.tsx (the "activity" TabsContent placeholder; existing tab wiring)
    - src/features/reports/activity.ts (getBorrowingActivity + ActivityPoint from Task 1)
  </read_first>
  <action>
    Create src/features/reports/BorrowingActivityChart.tsx with "use client". Import from recharts: ResponsiveContainer, LineChart, Line, XAxis, YAxis, Tooltip, Legend, CartesianGrid. Props: `{ initialData: ActivityPoint[]; initialFrom: string; initialTo: string }` (import ActivityPoint from ./activity). State: data (init initialData), fromDate, toDate, useTransition [isPending, startTransition].
    Filter bar identical in structure to PopularBooksTable: From/To date Inputs (w-36, labels "From"/"To") + a `<Button variant="default">Apply Filter</Button>` that calls startTransition(async () => { const r = await getBorrowingActivity({ fromDate, toDate }); if (r.success) setData(r.data); }); disable inputs/button while isPending.
    Chart card: `<Card>` with `<CardHeader><CardTitle>Borrowing Activity</CardTitle></CardHeader>` and `<CardContent>`. Inside CardContent:
    - While isPending: render `<Skeleton className="h-[320px] w-full rounded-md" />`.
    - Else if data.length === 0 OR every point has loanCount 0 and returnCount 0: render empty state `<div className="py-12 text-center">` with `<p className="text-base font-semibold">No activity data</p>` and `<p className="text-sm text-muted-foreground mt-1">No loans or returns recorded in the selected date range. Try a wider range.</p>`.
    - Else: `<figure>` containing `<figcaption className="sr-only">Borrowing activity chart showing loans issued and returned over time</figcaption>` and `<ResponsiveContainer width="100%" height={320}>` wrapping `<LineChart data={data}>` with: CartesianGrid; XAxis dataKey="date" tick={{ fontSize: 12 }} and interval set so that when data.length > 14 only every 7th label shows (interval={6}) else interval={0}; YAxis allowDecimals={false} tick={{ fontSize: 12 }}; Tooltip contentStyle={{ fontSize: "12px" }}; Legend; one `<Line type="monotone" dataKey="loanCount" name="Loans Issued" stroke="var(--chart-1)" />` and one `<Line type="monotone" dataKey="returnCount" name="Loans Returned" stroke="var(--chart-2)" />`. Use the series `name` props so the legend reads "Loans Issued" / "Loans Returned".
    In src/app/(app)/reports/page.tsx: reuse (or recompute) the fromStr/toStr default-30-day strings; call `const activityResult = await getBorrowingActivity({ fromDate: fromStr, toDate: toStr })` (import from @/features/reports/activity); `const activityData = activityResult.success ? activityResult.data : []`. Replace the "activity" TabsContent placeholder with `<BorrowingActivityChart initialData={activityData} initialFrom={fromStr} initialTo={toStr} />`. Leave overdue, popular, and fines tabs unchanged.
  </action>
  <verify>
    <automated>cd "C:/Users/ASUS/library-management-system" && npx tsc --noEmit -p tsconfig.json && grep -q "use client" src/features/reports/BorrowingActivityChart.tsx && grep -q "recharts" src/features/reports/BorrowingActivityChart.tsx && grep -q "Loans Issued" src/features/reports/BorrowingActivityChart.tsx && grep -q "var(--chart-1)" src/features/reports/BorrowingActivityChart.tsx && grep -q "No activity data" src/features/reports/BorrowingActivityChart.tsx && grep -q "getBorrowingActivity" src/app/\(app\)/reports/page.tsx</automated>
  </verify>
  <done>tsc passes; Borrowing Activity tab renders a recharts LineChart with two named series (Loans Issued / Loans Returned) over the default 30-day range, From/To + Apply Filter refetch, Skeleton while pending, and the empty state when no activity; chart colors use var(--chart-1)/var(--chart-2).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → getBorrowingActivity Server Action | Authenticated request carrying user-supplied fromDate/toDate strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Elevation of Privilege | getBorrowingActivity | mitigate | requireRole("LIBRARIAN") before DB access; FORBIDDEN return verified by unit Test 1 |
| T-05-11 | Tampering / Injection | fromDate/toDate params | mitigate | Safe date parse (new Date + isNaN guard); invalid input falls back to defaults; dates passed as Prisma Date objects (parameterized), never concatenated — verified by unit Test 4 |
| T-05-12 | Denial of Service | per-day expansion of huge ranges | mitigate | Aggregation buckets are bounded by the date window; only two date-bounded findMany selects (issuedAt / returnedAt) are run; no per-row N+1 |
</threat_model>

<verification>
- `npx vitest run tests/unit/report-activity.test.ts` — all pass.
- `npx tsc --noEmit` — no type errors.
- Manual: as LIBRARIAN, Borrowing Activity tab shows a two-series line chart for last 30 days; changing dates + Apply Filter refetches; an empty range shows "No activity data".
- Full suite: `npx vitest run` — all prior + new report tests green (regression check for the phase).
</verification>

<success_criteria>
- RPT-03 satisfied: librarian views an interactive borrowing-activity chart of loans issued vs returned over time.
- Phase 5 complete: all four report tabs (RPT-01..04) are functional at /reports.
- getBorrowingActivity enforces LIBRARIAN-only access with safe date parsing.
</success_criteria>

<output>
Create `.planning/phases/05-reports-analytics/05-04-SUMMARY.md` when done.
</output>

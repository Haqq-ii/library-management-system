---
phase: 05-reports-analytics
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/features/reports/overdue.ts
  - src/features/reports/OverdueLoansTable.tsx
  - src/app/(app)/reports/page.tsx
  - tests/unit/report-overdue.test.ts
autonomous: true
requirements: [RPT-01]
must_haves:
  truths:
    - "Librarian opens the Overdue Loans tab and sees every currently-overdue loan in a table"
    - "Each row shows member name, book title, due date, and days late"
    - "Rows are sorted by days late descending by default (most overdue first)"
    - "Librarian can re-sort by Member Name and by Title via column header buttons"
    - "When no loans are overdue, the tab shows 'No overdue loans'"
  artifacts:
    - path: "src/features/reports/overdue.ts"
      provides: "getOverdueLoans Server Action"
      exports: ["getOverdueLoans"]
      contains: "requireRole(\"LIBRARIAN\")"
    - path: "src/features/reports/OverdueLoansTable.tsx"
      provides: "Client table with header sort + days-late highlight"
      contains: "use client"
    - path: "tests/unit/report-overdue.test.ts"
      provides: "Unit tests for getOverdueLoans auth + overdue filter + daysLate"
  key_links:
    - from: "src/app/(app)/reports/page.tsx"
      to: "src/features/reports/overdue.ts"
      via: "getOverdueLoans call, result passed to OverdueLoansTable"
      pattern: "getOverdueLoans"
    - from: "src/features/reports/OverdueLoansTable.tsx"
      to: "props.rows"
      via: "client-side useState sort"
      pattern: "useState"
---

<objective>
Deliver RPT-01: the Overdue Loans report. Add a `getOverdueLoans` Server Action that returns all currently-overdue active loans with member name, book title, due date, and computed days-late; add a client `OverdueLoansTable` with default days-late-descending sort and re-sortable column headers; wire it into the Overdue Loans tab of /reports (replacing the placeholder).

Purpose: After this plan a librarian can see overdue exposure at a glance — who has what, how late, sortable. This is a complete vertical slice on top of the page shell from Plan 01.
Output: getOverdueLoans action, OverdueLoansTable component, wired Overdue Loans tab, unit tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-reports-analytics/05-UI-SPEC.md
@.planning/phases/05-reports-analytics/05-01-SUMMARY.md
@src/features/audit/actions.ts
@src/features/audit/AuditTable.tsx
@src/features/fines/FinesTable.tsx
@src/app/(app)/reports/page.tsx
@prisma/schema.prisma
@tests/unit/fine-actions.test.ts
</context>

<artifacts_produced>
Symbols this plan creates:
- `getOverdueLoans()` — Server Action in `src/features/reports/overdue.ts` returning `ActionResult<OverdueLoanRow[]>`
- `OverdueLoanRow` — exported type `{ id: string; memberName: string; bookTitle: string; dueAt: string; daysLate: number }`
- `OverdueLoansTable` — client component in `src/features/reports/OverdueLoansTable.tsx`
- Modifies `src/app/(app)/reports/page.tsx`: replaces the "overdue" TabsContent placeholder with `<OverdueLoansTable rows={...} />`
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getOverdueLoans Server Action with unit tests</name>
  <files>src/features/reports/overdue.ts, tests/unit/report-overdue.test.ts</files>
  <read_first>
    - src/features/audit/actions.ts (ActionResult type, "use server", requireRole try/catch, prisma import, include nested relations pattern)
    - prisma/schema.prisma (Loan model: status LoanStatus, dueAt Timestamptz, returnedAt nullable; copy → BookCopy → book → Book.title; member → Member → user → User.name)
    - src/features/notifications/actions.ts (nested member→user name resolution pattern; Date handling)
    - tests/unit/fine-actions.test.ts (vi.mock @/lib/require-role and @/lib/db, beforeEach clearAllMocks, FORBIDDEN test)
  </read_first>
  <behavior>
    - Test 1: requireRole rejects → returns { success: false, error: "FORBIDDEN" }, no DB query.
    - Test 2: LIBRARIAN role with two mocked loans (dueAt 10 days ago and 3 days ago, both returnedAt null) → returns success with two rows, each having memberName, bookTitle, dueAt (ISO string), and daysLate computed as whole days between dueAt and "now" (UTC, floor).
    - Test 3: a loan with returnedAt set or dueAt in the future is excluded by the query filter (assert the prisma.loan.findMany where clause includes returnedAt: null and dueAt: { lt: <now> }).
    - Test 4: dueAt in the returned rows is a string (ISO), not a Date object (serialization rule).
  </behavior>
  <action>
    Create src/features/reports/overdue.ts with "use server". Import requireRole, prisma, and re-declare or import ActionResult (mirror audit/actions.ts). Export type `OverdueLoanRow = { id: string; memberName: string; bookTitle: string; dueAt: string; daysLate: number }`.
    Implement `getOverdueLoans(): Promise<ActionResult<OverdueLoanRow[]>>`:
    - requireRole("LIBRARIAN") in try/catch → FORBIDDEN return on throw.
    - Query prisma.loan.findMany with where { returnedAt: null, dueAt: { lt: new Date() } } (a loan is overdue when not yet returned and past due). Include copy: { include: { book: true } } and member: { include: { user: true } }. orderBy dueAt asc (oldest-due first; final sort is client-side anyway).
    - Map each loan to OverdueLoanRow: id; memberName = loan.member.user.name; bookTitle = loan.copy.book.title; dueAt = loan.dueAt.toISOString(); daysLate = Math.floor((now.getTime() - loan.dueAt.getTime()) / 86400000) using a single `now = new Date()` captured before the query for consistency.
    - Wrap DB in try/catch → console.error("[getOverdueLoans]", err) + return { success:false, error:"DB_ERROR" }.
    Create tests/unit/report-overdue.test.ts following fine-actions.test.ts mock style: mock requireRole and prisma (prisma.loan.findMany as vi.fn). Cover the four behavior cases. For Test 3, assert findMany was called with an object whose where.returnedAt === null and where.dueAt has an `lt` Date.
  </action>
  <verify>
    <automated>cd "C:/Users/ASUS/library-management-system" && npx vitest run tests/unit/report-overdue.test.ts</automated>
  </verify>
  <done>All report-overdue.test.ts tests pass; getOverdueLoans enforces LIBRARIAN, filters to unreturned past-due loans, returns ISO-string dueAt and integer daysLate.</done>
</task>

<task type="auto">
  <name>Task 2: OverdueLoansTable client component and tab wiring</name>
  <files>src/features/reports/OverdueLoansTable.tsx, src/app/(app)/reports/page.tsx</files>
  <read_first>
    - src/features/audit/AuditTable.tsx ("use client", Table/TableHeader/TableRow/TableHead/TableBody/TableCell usage, empty-state cell pattern py-12 with heading text-base font-semibold + body text-sm text-muted-foreground, useState)
    - src/features/fines/FinesTable.tsx (table row rendering conventions)
    - .planning/phases/05-reports-analytics/05-UI-SPEC.md (RPT-01 Overdue Loans Tab: columns Member | Book Title | Due Date | Days Late; default sort days late desc; sort header buttons with aria-sort + ChevronUp/ChevronDown 16px; bg-red-50 row, text-red-600 font-medium days-late cell; min-h-[44px] sort triggers; empty state "No overdue loans" / "All loans are currently on time.")
    - src/app/(app)/reports/page.tsx (the "overdue" TabsContent placeholder to replace; existing getFineSummary wiring)
    - src/features/reports/overdue.ts (getOverdueLoans + OverdueLoanRow from Task 1)
  </read_first>
  <action>
    Create src/features/reports/OverdueLoansTable.tsx with "use client". Props: `{ rows: OverdueLoanRow[] }` (import OverdueLoanRow type from ./overdue). Import ChevronUp, ChevronDown from lucide-react.
    State: `sortKey` ("daysLate" | "memberName" | "bookTitle", default "daysLate") and `sortDir` ("asc" | "desc", default "desc"). Derive a sorted copy of rows via useMemo/useState: numeric compare for daysLate, localeCompare for memberName/bookTitle, direction-aware.
    Header cells for Member, Book Title, Due Date, Days Late. The Member, Title, and Days Late headers are `<button>` elements (min-h-[44px]) that toggle sort: clicking a new key sets that key with default dir (desc for daysLate, asc for names); clicking the active key flips sortDir. Each sort button sets `aria-sort` to "ascending"/"descending" when active else "none", renders the label plus a ChevronUp or ChevronDown icon (h-4 w-4, aria-hidden="true") reflecting current dir, and uses aria-label "Sort ascending"/"Sort descending". Due Date header is plain text (not sortable).
    Body: each row uses `bg-red-50`; Due Date cell formats dueAt via new Date(row.dueAt).toLocaleDateString("en-US", { timeZone:"UTC", month:"short", day:"numeric", year:"numeric" }); Days Late cell uses `text-red-600 font-medium` showing the integer.
    Empty state (rows.length === 0): single TableCell colSpan={4} className="text-center py-12" with heading `<p className="text-base font-semibold">No overdue loans</p>` and body `<p className="text-sm text-muted-foreground mt-1">All loans are currently on time.</p>`.
    Wrap table in `<div className="rounded-md border">` matching AuditTable.
    In src/app/(app)/reports/page.tsx: call `const overdueResult = await getOverdueLoans()` (import from @/features/reports/overdue); derive `const overdueRows = overdueResult.success ? overdueResult.data : []`. Replace the "overdue" TabsContent placeholder with `<OverdueLoansTable rows={overdueRows} />`. Leave popular/activity placeholders and the fines tab unchanged.
  </action>
  <verify>
    <automated>cd "C:/Users/ASUS/library-management-system" && npx tsc --noEmit -p tsconfig.json && grep -q "use client" src/features/reports/OverdueLoansTable.tsx && grep -q "aria-sort" src/features/reports/OverdueLoansTable.tsx && grep -q "No overdue loans" src/features/reports/OverdueLoansTable.tsx && grep -q "getOverdueLoans" src/app/\(app\)/reports/page.tsx</automated>
  </verify>
  <done>tsc passes; Overdue Loans tab renders a sortable table (days-late desc default, re-sortable by member/title with aria-sort + chevrons), red-highlighted rows, and the empty state when no overdue loans exist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → getOverdueLoans Server Action | Authenticated request for overdue-loan data (includes member names + book titles) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Elevation of Privilege | getOverdueLoans | mitigate | requireRole("LIBRARIAN") before any DB access; FORBIDDEN return verified by unit Test 1 |
| T-05-05 | Information Disclosure | overdue rows (member names) | mitigate | Action is librarian-only; returns only name + title + due/days-late, no contact PII or member ids exposed to non-librarians |
| T-05-06 | Tampering | sort/filter on table | accept | Sorting is client-side over an already-authorized result set; no user input reaches the DB query (no injectable parameters in getOverdueLoans) |
</threat_model>

<verification>
- `npx vitest run tests/unit/report-overdue.test.ts` — all pass.
- `npx tsc --noEmit` — no type errors.
- Manual: as LIBRARIAN, Overdue Loans tab lists overdue loans sorted days-late desc; clicking Member/Title headers re-sorts and toggles aria-sort; with no overdue loans the empty state shows.
</verification>

<success_criteria>
- RPT-01 satisfied: librarian views all currently-overdue loans, sortable by days late (and member/title), with member name and book title visible.
- Overdue filter is correct (returnedAt null AND dueAt < now).
- getOverdueLoans enforces LIBRARIAN-only access and serializes dates to ISO strings.
</success_criteria>

<output>
Create `.planning/phases/05-reports-analytics/05-02-SUMMARY.md` when done.
</output>

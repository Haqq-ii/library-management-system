---
phase: 05-reports-analytics
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - src/features/reports/popular.ts
  - src/features/reports/PopularBooksTable.tsx
  - src/app/(app)/reports/page.tsx
  - tests/unit/report-popular.test.ts
autonomous: true
requirements: [RPT-02]
must_haves:
  truths:
    - "Librarian opens the Popular Books tab and sees the most-borrowed titles for the selected date range"
    - "Each row shows rank, title, author, and borrow count, ordered by borrow count descending"
    - "Default date range is the last 30 days"
    - "Librarian can change From/To dates and click Apply Filter to refetch"
    - "When no loans were issued in the range, the tab shows 'No borrowing data'"
  artifacts:
    - path: "src/features/reports/popular.ts"
      provides: "getPopularBooks Server Action with safe date-range parsing"
      exports: ["getPopularBooks"]
      contains: "requireRole(\"LIBRARIAN\")"
    - path: "src/features/reports/PopularBooksTable.tsx"
      provides: "Client table with date-range filter + useTransition refetch"
      contains: "use client"
    - path: "tests/unit/report-popular.test.ts"
      provides: "Unit tests for auth, date filter, ranking, invalid-date handling"
  key_links:
    - from: "src/app/(app)/reports/page.tsx"
      to: "src/features/reports/popular.ts"
      via: "getPopularBooks initial call, result passed to PopularBooksTable"
      pattern: "getPopularBooks"
    - from: "src/features/reports/PopularBooksTable.tsx"
      to: "src/features/reports/popular.ts"
      via: "getPopularBooks refetch inside useTransition on Apply Filter"
      pattern: "startTransition"
---

<objective>
Deliver RPT-02: the Popular Books report. Add a `getPopularBooks` Server Action that ranks book titles by loan count within a date range (default last 30 days, safe date parsing); add a client `PopularBooksTable` with From/To date inputs and an Apply Filter button that refetches via useTransition; wire it into the Popular Books tab of /reports.

Purpose: After this plan a librarian can see which titles circulate most over any chosen window — informing acquisitions and shelving. Complete vertical slice on top of the page shell.
Output: getPopularBooks action, PopularBooksTable component, wired Popular Books tab, unit tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-reports-analytics/05-UI-SPEC.md
@.planning/phases/05-reports-analytics/05-02-SUMMARY.md
@src/features/audit/actions.ts
@src/features/audit/AuditTable.tsx
@src/app/(app)/reports/page.tsx
@prisma/schema.prisma
@tests/unit/fine-actions.test.ts
</context>

<artifacts_produced>
Symbols this plan creates:
- `getPopularBooks(params)` — Server Action in `src/features/reports/popular.ts`, params `{ fromDate?: string; toDate?: string }`, returns `ActionResult<PopularBookRow[]>`
- `PopularBookRow` — exported type `{ bookId: string; title: string; author: string; borrowCount: number }`
- `PopularBooksTable` — client component in `src/features/reports/PopularBooksTable.tsx`
- Modifies `src/app/(app)/reports/page.tsx`: replaces the "popular" TabsContent placeholder with `<PopularBooksTable initialRows={...} initialFrom={...} initialTo={...} />`
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getPopularBooks Server Action with unit tests</name>
  <files>src/features/reports/popular.ts, tests/unit/report-popular.test.ts</files>
  <read_first>
    - src/features/audit/actions.ts (ActionResult, "use server", requireRole try/catch, SAFE date parsing pattern: new Date(param), isNaN(d.getTime()) guard, toDate set to end-of-day via setUTCHours(23,59,59,999))
    - prisma/schema.prisma (Loan model: issuedAt Timestamptz, copy → BookCopy.bookId → Book.title + Book.author relation → Author.name)
    - tests/unit/fine-actions.test.ts (vi.mock pattern, beforeEach clearAllMocks, FORBIDDEN test)
  </read_first>
  <behavior>
    - Test 1: requireRole rejects → { success:false, error:"FORBIDDEN" }, no DB query.
    - Test 2: LIBRARIAN with no fromDate/toDate → query uses a default 30-day window (assert the where.issuedAt.gte is ~30 days before now); returns ranked rows.
    - Test 3: provided fromDate/toDate → where.issuedAt.gte = parsed from, where.issuedAt.lte = parsed to with end-of-day time; rows ordered by borrowCount descending.
    - Test 4: invalid date string (e.g. "not-a-date") is ignored (falls back to default for that bound) — no throw, no NaN in the query.
    - Test 5: borrow counts come from grouping loans by book; a book borrowed 3 times ranks above one borrowed 1 time.
  </behavior>
  <action>
    Create src/features/reports/popular.ts with "use server". Import requireRole, prisma, ActionResult. Export type `PopularBookRow = { bookId: string; title: string; author: string; borrowCount: number }`.
    Implement `getPopularBooks(params: { fromDate?: string; toDate?: string }): Promise<ActionResult<PopularBookRow[]>>`:
    - requireRole("LIBRARIAN") in try/catch → FORBIDDEN.
    - Compute window: default `from` = 30 days before now, default `to` = now. If params.fromDate parses to a valid Date (new Date + !isNaN(getTime())) use it; else default. Same for toDate, and when valid call setUTCHours(23,59,59,999) for inclusive end-of-day (matches audit/actions.ts). Reject invalid strings by falling back to defaults (never pass NaN dates to Prisma).
    - Count loans per book within [from, to] on Loan.issuedAt. Use prisma.loan.groupBy by copyId is insufficient (need book-level) — instead query prisma.loan.findMany where { issuedAt: { gte: from, lte: to } } include copy: { include: { book: { include: { author: true } } } }, then aggregate counts per book.id in JS into a Map keyed by bookId accumulating { title, author: book.author.name, borrowCount }. (A raw groupBy on bookId is acceptable only if a bookId column is reachable; Loan has copyId, so the in-JS aggregation over the included book relation is the correct path.)
    - Produce PopularBookRow[] sorted by borrowCount descending; limit to top 50.
    - try/catch DB → console.error("[getPopularBooks]", err) + { success:false, error:"DB_ERROR" }.
    Create tests/unit/report-popular.test.ts (fine-actions.test.ts style): mock requireRole and prisma.loan.findMany. Cover the five behavior cases; for Test 4 assert the where.issuedAt bounds are valid Date instances (not NaN).
  </action>
  <verify>
    <automated>cd "C:/Users/ASUS/library-management-system" && npx vitest run tests/unit/report-popular.test.ts</automated>
  </verify>
  <done>All report-popular.test.ts tests pass; getPopularBooks enforces LIBRARIAN, applies a safe default-30-day window, ignores invalid dates, and returns books ranked by borrow count descending.</done>
</task>

<task type="auto">
  <name>Task 2: PopularBooksTable client component and tab wiring</name>
  <files>src/features/reports/PopularBooksTable.tsx, src/app/(app)/reports/page.tsx</files>
  <read_first>
    - src/features/audit/AuditTable.tsx ("use client", date Input filter bar with label text-xs text-muted-foreground font-medium + Input type="date" w-36, useTransition + startTransition refetch, isPending disabling, Button, empty-state cell, rounded-md border wrapper)
    - .planning/phases/05-reports-analytics/05-UI-SPEC.md (RPT-02 Popular Books Tab: filter bar From [date] To [date] + Apply Filter button; columns Rank | Title | Author | Borrow Count; rank w-12 text-sm text-muted-foreground; Apply Filter triggers useTransition with isPending spinner; empty state "No borrowing data" / "No loans were issued in the selected date range. Try a wider range."; column header copy "Borrow Count")
    - src/app/(app)/reports/page.tsx (the "popular" TabsContent placeholder; existing tab wiring)
    - src/features/reports/popular.ts (getPopularBooks + PopularBookRow from Task 1)
  </read_first>
  <action>
    Create src/features/reports/PopularBooksTable.tsx with "use client". Props: `{ initialRows: PopularBookRow[]; initialFrom: string; initialTo: string }` (import PopularBookRow from ./popular). State: rows (init initialRows), fromDate (init initialFrom), toDate (init initialTo), useTransition [isPending, startTransition].
    Filter bar matching AuditTable: two date Input (type="date", className "w-36", value bound to fromDate/toDate, disabled while isPending) each with a `<label className="text-xs text-muted-foreground font-medium">` reading "From" / "To"; plus a primary `<Button variant="default">` labeled "Apply Filter" (disabled while isPending) that calls a handleApply which runs startTransition(async () => { const r = await getPopularBooks({ fromDate, toDate }); if (r.success) setRows(r.data); }). Show isPending feedback on the button (e.g. "Applying…" text or a spinner).
    Table inside `<div className="rounded-md border">` with columns Rank | Title | Author | Borrow Count. Rank cell = index+1 with className "w-12 text-sm text-muted-foreground". Borrow Count header label exactly "Borrow Count".
    Empty state (rows.length === 0): TableCell colSpan={4} text-center py-12, heading `<p className="text-base font-semibold">No borrowing data</p>`, body `<p className="text-sm text-muted-foreground mt-1">No loans were issued in the selected date range. Try a wider range.</p>`.
    In src/app/(app)/reports/page.tsx: compute default range strings on the server — `const today = new Date(); const toStr = today.toISOString().slice(0,10); const fromStr = new Date(today.getTime() - 30*86400000).toISOString().slice(0,10);` Call `const popularResult = await getPopularBooks({ fromDate: fromStr, toDate: toStr })` (import from @/features/reports/popular); `const popularRows = popularResult.success ? popularResult.data : []`. Replace the "popular" TabsContent placeholder with `<PopularBooksTable initialRows={popularRows} initialFrom={fromStr} initialTo={toStr} />`. Leave overdue, activity, and fines tabs unchanged.
  </action>
  <verify>
    <automated>cd "C:/Users/ASUS/library-management-system" && npx tsc --noEmit -p tsconfig.json && grep -q "use client" src/features/reports/PopularBooksTable.tsx && grep -q "Apply Filter" src/features/reports/PopularBooksTable.tsx && grep -q "Borrow Count" src/features/reports/PopularBooksTable.tsx && grep -q "No borrowing data" src/features/reports/PopularBooksTable.tsx && grep -q "getPopularBooks" src/app/\(app\)/reports/page.tsx</automated>
  </verify>
  <done>tsc passes; Popular Books tab shows ranked title/author/borrow-count table for the default last-30-days range, From/To inputs + Apply Filter refetch via useTransition, and the empty state when the range has no loans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → getPopularBooks Server Action | Authenticated request carrying user-supplied fromDate/toDate strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Elevation of Privilege | getPopularBooks | mitigate | requireRole("LIBRARIAN") before DB access; FORBIDDEN return verified by unit Test 1 |
| T-05-08 | Tampering / Injection | fromDate/toDate params | mitigate | Dates parsed via new Date() + isNaN(getTime()) guard; invalid strings fall back to defaults; values bound as Prisma Date objects (parameterized), never string-concatenated into SQL — verified by unit Test 4 |
| T-05-09 | Denial of Service | unbounded result set | mitigate | Result limited to top 50 rows; query bounded by date window |
</threat_model>

<verification>
- `npx vitest run tests/unit/report-popular.test.ts` — all pass.
- `npx tsc --noEmit` — no type errors.
- Manual: as LIBRARIAN, Popular Books tab shows ranked titles for last 30 days; changing dates + Apply Filter refetches; a future-only range shows the empty state.
</verification>

<success_criteria>
- RPT-02 satisfied: librarian views most-borrowed titles over a selected date range.
- Safe date parsing prevents NaN/injection; default window is last 30 days.
- getPopularBooks enforces LIBRARIAN-only access.
</success_criteria>

<output>
Create `.planning/phases/05-reports-analytics/05-03-SUMMARY.md` when done.
</output>

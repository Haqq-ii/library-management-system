---
phase: 05-reports-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/components/layout/AppSidebar.tsx
  - src/features/reports/actions.ts
  - src/features/reports/FineSummaryCards.tsx
  - src/app/(app)/reports/page.tsx
  - tests/unit/report-fine-summary.test.ts
autonomous: true
requirements: [RPT-04]
must_haves:
  truths:
    - "Librarian sees a 'Reports' link in the sidebar and clicking it loads /reports"
    - "A member navigating to /reports is redirected to /dashboard"
    - "The /reports page shows four tabs: Overdue Loans, Popular Books, Borrowing Activity, Fine Summary"
    - "The Fine Summary tab shows total fines recorded, total waived, and total outstanding in three stat cards"
    - "Outstanding equals Recorded minus Waived"
  artifacts:
    - path: "src/features/reports/actions.ts"
      provides: "getFineSummary Server Action + shared ActionResult type"
      exports: ["getFineSummary", "ActionResult"]
      contains: "requireRole(\"LIBRARIAN\")"
    - path: "src/features/reports/FineSummaryCards.tsx"
      provides: "Three-card fine summary display"
      contains: "Total Fines Recorded"
    - path: "src/app/(app)/reports/page.tsx"
      provides: "Reports page with Tabs shell + auth guard"
      contains: "TabsTrigger"
    - path: "tests/unit/report-fine-summary.test.ts"
      provides: "Unit tests for getFineSummary auth + aggregation"
  key_links:
    - from: "src/app/(app)/reports/page.tsx"
      to: "src/features/reports/actions.ts"
      via: "getFineSummary call in Server Component"
      pattern: "getFineSummary"
    - from: "src/components/layout/AppSidebar.tsx"
      to: "/reports"
      via: "LIBRARIAN_NAV entry"
      pattern: "/reports"
---

<objective>
Stand up the Reports feature: install the recharts dependency, add the librarian-only `/reports` page as a Server Component with a four-tab shell, add the sidebar nav entry, and deliver the first complete report — RPT-04 Fine Summary (all-time totals).

Purpose: After this plan a librarian can navigate to /reports and see real fine-collection numbers. This is the thinnest end-to-end vertical slice for the Reports phase and establishes the page shell, action-file location, and auth pattern that the three remaining report slices plug into.
Output: recharts in package.json, /reports route, sidebar link, getFineSummary Server Action, FineSummaryCards component, unit tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-reports-analytics/05-UI-SPEC.md
@src/features/audit/actions.ts
@src/features/fines/FinesTable.tsx
@src/app/(app)/fines/page.tsx
@src/features/dashboard/DashboardStats.tsx
@src/components/layout/AppSidebar.tsx
@src/components/ui/tabs.tsx
@src/components/ui/card.tsx
@prisma/schema.prisma
@tests/unit/fine-actions.test.ts
</context>

<artifacts_produced>
Symbols this plan creates (consumed by later plans / executor):
- `getFineSummary()` — Server Action in `src/features/reports/actions.ts` returning `ActionResult<{ recorded: number; waived: number; outstanding: number }>`
- `ActionResult<T>` — shared discriminated-union type exported from `src/features/reports/actions.ts` (mirrors the audit/notifications pattern). Later report action files re-declare or import this type.
- `FineSummaryCards` — React Server Component in `src/features/reports/FineSummaryCards.tsx`
- New route: `src/app/(app)/reports/page.tsx` (the Reports page; later plans add tab content to it)
- New file: `src/features/reports/` directory (feature folder for all report actions/components)
- recharts ^2 added to `package.json` dependencies
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Install recharts and add Reports sidebar nav entry</name>
  <files>package.json, package-lock.json, src/components/layout/AppSidebar.tsx</files>
  <read_first>
    - src/components/layout/AppSidebar.tsx (current LIBRARIAN_NAV array and lucide-react import block)
    - package.json (confirm recharts is NOT already present)
    - CLAUDE.md (recharts is the vetted/recommended chart library — recharts 2.x, SVG-native)
  </read_first>
  <action>
    Run `npm install recharts` (this resolves to recharts ^2.x — the vetted library named in CLAUDE.md's recommended stack; no separate legitimacy checkpoint needed because the stack decision is already locked in CLAUDE.md).
    In AppSidebar.tsx: add `BarChart2` to the existing `lucide-react` import statement (the block that currently imports LayoutDashboard, BookOpen, Users, BookMarked, Search, User, Menu, LogOut, Receipt, ClipboardList, Bell).
    Append exactly one entry to the END of the `LIBRARIAN_NAV` array, after the existing "Notification Log" entry: `{ href: "/reports", label: "Reports", icon: BarChart2 }`.
    Do NOT modify `MEMBER_NAV` — reports are librarian-only.
  </action>
  <verify>
    <automated>cd "C:/Users/ASUS/library-management-system" && node -e "const p=require('./package.json'); if(!p.dependencies.recharts) process.exit(1)" && grep -q "BarChart2" src/components/layout/AppSidebar.tsx && grep -q "href: \"/reports\"" src/components/layout/AppSidebar.tsx</automated>
  </verify>
  <done>recharts appears in package.json dependencies; AppSidebar imports BarChart2 and LIBRARIAN_NAV contains a /reports entry labeled "Reports"; MEMBER_NAV unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: getFineSummary Server Action with unit tests</name>
  <files>src/features/reports/actions.ts, tests/unit/report-fine-summary.test.ts</files>
  <read_first>
    - src/features/audit/actions.ts (ActionResult type shape, requireRole try/catch returning {success:false,error}, "use server" directive, prisma import from @/lib/db)
    - src/app/(app)/fines/page.tsx (Prisma Decimal -> Number serialization via Number(f.amount))
    - prisma/schema.prisma (Fine model: amount Decimal, status FineStatus enum UNPAID|PAID|WAIVED)
    - tests/unit/fine-actions.test.ts (vi.mock pattern for @/lib/require-role and @/lib/db, beforeEach clearAllMocks, FORBIDDEN assertion)
  </read_first>
  <behavior>
    - Test 1: when requireRole rejects with Error("FORBIDDEN"), getFineSummary returns { success: false, error: "FORBIDDEN" } and never queries the DB.
    - Test 2: with LIBRARIAN role and mocked aggregates (recorded total 100, waived total 30), returns { success: true, data: { recorded: 100, waived: 30, outstanding: 70 } } — outstanding = recorded - waived.
    - Test 3: with zero fines (all aggregates null/0), returns data { recorded: 0, waived: 0, outstanding: 0 } (null Decimal sums coerce to 0, not NaN).
    - Test 4: returned numeric fields are JS `number` type (not Prisma Decimal objects).
  </behavior>
  <action>
    Create src/features/reports/actions.ts with "use server" at top. Import requireRole from @/lib/require-role and prisma from @/lib/db. Export the shared `ActionResult<T>` discriminated union exactly as in audit/actions.ts (success:true,data:T | success:false,error:string).
    Implement `getFineSummary(): Promise<ActionResult<{ recorded: number; waived: number; outstanding: number }>>`:
    - First call `await requireRole("LIBRARIAN")` inside try/catch; on throw return { success: false, error: err instanceof Error ? err.message : "FORBIDDEN" }.
    - Compute `recorded` = sum of `amount` across ALL fines (prisma.fine.aggregate { _sum: { amount: true } }, no status filter).
    - Compute `waived` = sum of `amount` where status === "WAIVED" (prisma.fine.aggregate with where: { status: "WAIVED" }).
    - Convert each Prisma Decimal sum to Number; coerce null sums to 0 via `Number(agg._sum.amount ?? 0)`.
    - `outstanding` = recorded - waived (computed in JS, not a third query).
    - Wrap DB calls in try/catch; on error console.error("[getFineSummary]", err) and return { success:false, error:"DB_ERROR" }.
    Create tests/unit/report-fine-summary.test.ts following tests/unit/fine-actions.test.ts: mock @/lib/require-role and @/lib/db (prisma.fine.aggregate as vi.fn), beforeEach vi.clearAllMocks, cover the four behavior cases above. Use mockResolvedValue to return { _sum: { amount: <Decimal-like number> } } per call.
  </action>
  <verify>
    <automated>cd "C:/Users/ASUS/library-management-system" && npx vitest run tests/unit/report-fine-summary.test.ts</automated>
  </verify>
  <done>All tests in report-fine-summary.test.ts pass; getFineSummary enforces LIBRARIAN, returns number-typed recorded/waived/outstanding, computes outstanding = recorded - waived, handles zero/null aggregates without NaN.</done>
</task>

<task type="auto">
  <name>Task 3: FineSummaryCards component and /reports page shell</name>
  <files>src/features/reports/FineSummaryCards.tsx, src/app/(app)/reports/page.tsx</files>
  <read_first>
    - src/app/(app)/notifications/page.tsx (Server Component auth guard: getSession, redirect /login if no session, redirect /dashboard if role !== "LIBRARIAN"; space-y-6 wrapper; h1 text-2xl font-semibold)
    - src/features/dashboard/DashboardStats.tsx (Card/CardHeader/CardTitle/CardContent usage, grid gap-4 md:grid-cols-3 layout, CardTitle text-sm font-medium text-muted-foreground)
    - src/components/ui/tabs.tsx (Tabs/TabsList/TabsTrigger/TabsContent API — base-ui, defaultValue prop, value per trigger/content)
    - .planning/phases/05-reports-analytics/05-UI-SPEC.md (Page Layout Contract for /reports; RPT-04 copy: "Total Fines Recorded", "Total Waived", "Total Outstanding", note "Outstanding = Recorded minus Waived"; badge classes bg-orange-100 text-orange-800 for waived, bg-red-100 text-red-800 for outstanding)
    - src/features/reports/actions.ts (getFineSummary signature from Task 2)
  </read_first>
  <action>
    Create src/features/reports/FineSummaryCards.tsx (Server Component, NO "use client"). Props: `{ recorded: number; waived: number; outstanding: number }`. Render `grid gap-4 md:grid-cols-3` with three Cards using Card/CardHeader/CardTitle/CardContent from @/components/ui/card:
    - Card 1 title "Total Fines Recorded", value `${recorded.toFixed(2)}` in `text-2xl font-semibold`, no badge.
    - Card 2 title "Total Waived", value `${waived.toFixed(2)}` in `text-2xl font-semibold`, plus a Badge with className "bg-orange-100 text-orange-800".
    - Card 3 title "Total Outstanding", value `${outstanding.toFixed(2)}` in `text-2xl font-semibold`, plus a Badge with className "bg-red-100 text-red-800".
    Below the grid render a note `<p className="text-sm text-muted-foreground">Outstanding = Recorded minus Waived</p>`. Currency format is exactly `$` + two decimals via toFixed(2).
    Create src/app/(app)/reports/page.tsx (Server Component, async). Auth guard identical to notifications/page.tsx: getSession via auth.api.getSession({ headers: await headers() }); redirect("/login") if no session; redirect("/dashboard") if session.user.role !== "LIBRARIAN".
    Call `const result = await getFineSummary()`; derive `{ recorded, waived, outstanding }` from result.data with `{ recorded:0, waived:0, outstanding:0 }` fallback when !result.success.
    Render `<div className="space-y-6">` containing `<h1 className="text-2xl font-semibold">Reports</h1>` and `<Tabs defaultValue="overdue">` with a TabsList of four TabsTrigger (values overdue/popular/activity/fines, labels "Overdue Loans"/"Popular Books"/"Borrowing Activity"/"Fine Summary") and four TabsContent. For this plan, the overdue/popular/activity TabsContent each render a placeholder `<p className="text-sm text-muted-foreground py-12 text-center">Coming soon</p>` (later plans replace these). The fines TabsContent renders `<FineSummaryCards recorded={recorded} waived={waived} outstanding={outstanding} />`.
  </action>
  <verify>
    <automated>cd "C:/Users/ASUS/library-management-system" && npx tsc --noEmit -p tsconfig.json && grep -q "Total Fines Recorded" src/features/reports/FineSummaryCards.tsx && grep -q "getFineSummary" src/app/\(app\)/reports/page.tsx && grep -q "Outstanding = Recorded minus Waived" src/features/reports/FineSummaryCards.tsx</automated>
  </verify>
  <done>tsc passes with no errors; /reports renders four tabs with the Fine Summary tab showing three stat cards and the outstanding-formula note; non-librarians are redirected; the other three tabs show placeholder content.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Server Component /reports | Authenticated user requests the reports page; role must be verified server-side |
| Server Action getFineSummary → DB | Aggregate query over Fine table; must not run for non-librarians |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Elevation of Privilege | /reports page.tsx | mitigate | Server Component checks session.user.role !== "LIBRARIAN" → redirect("/dashboard"); does not rely on middleware (CVE-2025-29927) |
| T-05-02 | Elevation of Privilege | getFineSummary Server Action | mitigate | Calls requireRole("LIBRARIAN") before any DB access; returns FORBIDDEN on failure; verified by unit Test 1 |
| T-05-03 | Information Disclosure | Fine aggregate response | mitigate | Returns only aggregate totals (no member PII, no individual fine rows); no IDOR surface — no per-record id accepted as input |
| T-05-SC | Tampering | npm install recharts | accept | recharts is the vetted chart library named in CLAUDE.md recommended stack (HIGH confidence, 2M+ weekly downloads); no untrusted/ASSUMED package introduced |
</threat_model>

<verification>
- `npm install recharts` succeeds and recharts is in package.json.
- `npx vitest run tests/unit/report-fine-summary.test.ts` — all pass.
- `npx tsc --noEmit` — no type errors.
- Manual: as LIBRARIAN, sidebar shows "Reports"; /reports loads four tabs; Fine Summary tab shows three $ totals where Outstanding = Recorded − Waived. As MEMBER, /reports redirects to /dashboard.
</verification>

<success_criteria>
- RPT-04 satisfied: librarian views fine summary (total recorded, waived, outstanding) at /reports → Fine Summary tab.
- recharts installed for downstream RPT-03 plan.
- /reports page shell + sidebar nav in place for downstream report slices.
- getFineSummary enforces LIBRARIAN-only access and serializes Decimal → number.
</success_criteria>

<output>
Create `.planning/phases/05-reports-analytics/05-01-SUMMARY.md` when done.
</output>

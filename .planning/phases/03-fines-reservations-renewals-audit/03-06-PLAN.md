---
phase: "03"
plan: "06"
type: execute
wave: 4
depends_on:
  - "03-04"
files_modified:
  - src/app/(app)/my-loans/page.tsx
  - src/features/loans/ActiveLoansClient.tsx
  - src/features/loans/CheckoutSheet.tsx
  - src/components/layout/AppSidebar.tsx
autonomous: true
requirements:
  - FINE-03
  - RES-04
  - RNW-01
  - RNW-02
  - RNW-03
  - RNW-04

must_haves:
  truths:
    - "Active loans table on /my-loans has a new Actions column with a Renew button on each active loan row"
    - "Clicking Renew calls renewLoan(loanId) via useTransition"
    - "Successful renewal shows toast: 'Loan renewed. New due date: [date].'"
    - "FINE_BLOCK error shows toast: 'Renewal blocked: $X.XX in unpaid fines.'"
    - "MAX_RENEWALS error shows toast: 'Renewal blocked: maximum renewals (N) reached.'"
    - "RESERVATION_BLOCK error shows toast: 'Renewal blocked: another member has a hold on this title.'"
    - "CheckoutSheet shows inline error paragraph when checkoutBook returns FINE_BLOCK: 'This member has $X.XX in outstanding fines. Clear fines before checkout.'"
    - "AppSidebar LIBRARIAN_NAV includes Fines (Receipt icon) and Audit Log (ClipboardList icon)"
    - "AppSidebar MEMBER_NAV My Reservations entry no longer has disabled: true"
  artifacts:
    - path: "src/features/loans/ActiveLoansClient.tsx"
      provides: "Client component for active loans section with Renew button and useTransition handler"
      exports: ["ActiveLoansClient"]
    - path: "src/app/(app)/my-loans/page.tsx"
      provides: "/my-loans page using ActiveLoansClient for active section"
    - path: "src/features/loans/CheckoutSheet.tsx"
      provides: "CheckoutSheet with inline FINE_BLOCK error display"
    - path: "src/components/layout/AppSidebar.tsx"
      provides: "AppSidebar with Fines, Audit Log in LIBRARIAN_NAV; My Reservations enabled in MEMBER_NAV"
  key_links:
    - from: "src/app/(app)/my-loans/page.tsx"
      to: "src/features/loans/ActiveLoansClient.tsx"
      via: "ActiveLoansClient component with loans prop"
      pattern: "ActiveLoansClient.*loans="
    - from: "src/features/loans/ActiveLoansClient.tsx"
      to: "src/features/loans/actions.ts"
      via: "renewLoan import"
      pattern: "import.*renewLoan.*from.*loans/actions"
    - from: "src/features/loans/CheckoutSheet.tsx"
      to: "checkoutBook error handling"
      via: "FINE_BLOCK error code parsed from result.error"
      pattern: "FINE_BLOCK"
---

<objective>
Wire the renewal UI into `/my-loans` (Renew button with all block-specific toast messages), add the FINE_BLOCK inline error to `CheckoutSheet`, and update `AppSidebar` with the new nav items. This is the final UI wiring plan for Phase 3.

Purpose: Close RNW-01–04 UI, complete FINE-03 checkout error display, activate sidebar navigation.
Output: ActiveLoansClient component, updated my-loans page, CheckoutSheet inline error, updated AppSidebar.
</objective>

<execution_context>
@C:/Users/ASUS/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/ASUS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-fines-reservations-renewals-audit/03-CONTEXT.md
@.planning/phases/03-fines-reservations-renewals-audit/03-UI-SPEC.md
@.planning/phases/03-fines-reservations-renewals-audit/03-PATTERNS.md
@.planning/phases/03-fines-reservations-renewals-audit/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ActiveLoansClient component + my-loans page update</name>
  <files>
    src/features/loans/ActiveLoansClient.tsx
    src/app/(app)/my-loans/page.tsx
  </files>
  <read_first>
    - src/app/(app)/my-loans/page.tsx (full file — read current active loans section structure to extract into ActiveLoansClient; understand existing table columns: Book, Author, Issued, Due, Status)
    - src/features/loans/LoansTable.tsx (useTransition + toast pattern, exact syntax for startTransition with async Server Action call)
    - src/features/loans/actions.ts (current state post-Plan 04 — verify renewLoan is exported and understand ActionResult shape for FINE_BLOCK, MAX_RENEWALS, RESERVATION_BLOCK errors)
    - .planning/phases/03-fines-reservations-renewals-audit/03-UI-SPEC.md (/my-loans Renew Button section — exact Button props, Actions column header, all toast messages per block type)
    - .planning/phases/03-fines-reservations-renewals-audit/03-PATTERNS.md (my-loans/page.tsx section — useTransition + toast pattern for renewLoan, Renew button, handleRenew function)
  </read_first>
  <action>
    PART A — Create `src/features/loans/ActiveLoansClient.tsx`:
    "use client" component. Props: `loans` array (the active loan data with copy.book.author included — same shape as currently in my-loans page active section).

    State: isPending from useTransition.

    handleRenew(loanId: string) function using startTransition:
    - Call renewLoan(loanId) from "@/features/loans/actions"
    - On result.success: compute dateStr = new Date(result.data.newDueAt).toLocaleDateString("en-US", { month: "short", day: "numeric", year: "numeric" }); toast.success(`Loan renewed. New due date: ${dateStr}.`)
    - On result.error starting with "FINE_BLOCK:": extract amount = result.error.split(":")[1]; toast.error(`Renewal blocked: $${amount} in unpaid fines.`)
    - On result.error starting with "MAX_RENEWALS:": extract max = result.error.split(":")[1]; toast.error(`Renewal blocked: maximum renewals (${max}) reached.`)
    - On result.error === "RESERVATION_BLOCK": toast.error("Renewal blocked: another member has a hold on this title.")
    - Otherwise: toast.error("Couldn't renew loan. Please try again.")

    Table structure: Replicate the existing active loans table from my-loans/page.tsx (rows with Book, Author, Issued, Due, Status columns), then ADD a new Actions column:
    - TableHead: `<TableHead className="w-24">Actions</TableHead>` after the Status column.
    - TableCell in each row: `<TableCell><Button size="sm" variant="outline" disabled={isPending} onClick={() => handleRenew(loan.id)}>Renew</Button></TableCell>`

    Preserve all existing row styling: overdue rows get `className={overdue ? "bg-red-50" : undefined}`, Due column gets `className={overdue ? "text-red-600 font-medium" : "text-sm"}`. Use the existing `isLoanOverdue` import from "@/features/loans/loan-display".

    Empty state: `<TableCell colSpan={6} className="text-center text-muted-foreground py-8">No active loans</TableCell>` (colSpan = 6 now, one more than before due to Actions column).

    PART B — Update `src/app/(app)/my-loans/page.tsx`:
    Read the current file. The active loans section currently renders an inline table. Replace the active loans table rendering with `<ActiveLoansClient loans={sortedActive} />`. Import ActiveLoansClient from "@/features/loans/ActiveLoansClient".

    The Loan History section (second half of the page) remains as a Server Component table — no changes needed there. Keep all existing data fetching, partitionLoans, sorting logic unchanged.

    The page stays a Server Component (no "use client" — only the ActiveLoansClient sub-component is client-side).
  </action>
  <verify>
    <automated>cd "C:/Users/ASUS/library-management-system" && npx tsc --noEmit 2>&1 | grep -E "ActiveLoansClient|my-loans/page" | wc -l</automated>
  </verify>
  <done>
    - src/features/loans/ActiveLoansClient.tsx exists with Renew button and all 4 toast cases
    - src/app/(app)/my-loans/page.tsx uses ActiveLoansClient for the active loans section
    - TypeScript compilation reports 0 errors in both files
    - Active loans table has 6 columns (Book, Author, Issued, Due, Status, Actions)
  </done>
  <acceptance_criteria>
    - `grep -c "export function ActiveLoansClient" src/features/loans/ActiveLoansClient.tsx` returns 1
    - `grep -c "FINE_BLOCK" src/features/loans/ActiveLoansClient.tsx` returns 1
    - `grep -c "MAX_RENEWALS" src/features/loans/ActiveLoansClient.tsx` returns 1
    - `grep -c "RESERVATION_BLOCK" src/features/loans/ActiveLoansClient.tsx` returns 1
    - `grep -c "Renewal blocked" src/features/loans/ActiveLoansClient.tsx` returns 3 (one per block type)
    - `grep -c "renewLoan" src/features/loans/ActiveLoansClient.tsx` returns at least 1 (import + call)
    - `grep -c "ActiveLoansClient" src/app/(app)/my-loans/page.tsx` returns at least 1
    - `npx tsc --noEmit 2>&1 | grep -E "ActiveLoansClient|my-loans/page"` returns empty (no TS errors)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: CheckoutSheet FINE_BLOCK inline error + AppSidebar nav additions</name>
  <files>
    src/features/loans/CheckoutSheet.tsx
    src/components/layout/AppSidebar.tsx
  </files>
  <read_first>
    - src/features/loans/CheckoutSheet.tsx (full file — read current onSubmit/handleConfirm logic to understand where checkoutBook result is handled; find where to add the FINE_BLOCK inline error paragraph)
    - src/components/layout/AppSidebar.tsx (full file — read LIBRARIAN_NAV array and MEMBER_NAV array to add Fines + Audit Log and remove disabled from My Reservations)
    - .planning/phases/03-fines-reservations-renewals-audit/03-UI-SPEC.md (CheckoutSheet FINE_BLOCK inline error spec: `<p className="text-sm text-destructive mt-2">` paragraph; AppSidebar Changes section: exact LIBRARIAN_NAV order, MEMBER_NAV My Reservations change; Receipt + ClipboardList icons)
    - .planning/phases/03-fines-reservations-renewals-audit/03-PATTERNS.md (AppSidebar.tsx section — exact import additions, LIBRARIAN_NAV final order)
  </read_first>
  <action>
    PART A — Modify `src/features/loans/CheckoutSheet.tsx`:
    Read the full CheckoutSheet component. Find the submit handler that calls checkoutBook. The current handler likely calls toast.error for failures. Add FINE_BLOCK-specific handling:

    1. Add state: `const [fineBlockError, setFineBlockError] = useState<string | null>(null)`.
    2. Reset fineBlockError to null when the sheet is closed (in handleOpenChange) and when a new member is selected (in handleSelectMember).
    3. In the checkoutBook result handler: if result.error starts with "FINE_BLOCK:", parse the amount (`result.error.split(":")[1]`) and set `setFineBlockError(\`This member has $${amount} in outstanding fines. Clear fines before checkout.\`)`. Do NOT show a toast for FINE_BLOCK (inline error is preferred per UI-SPEC). For all other errors, continue with the existing toast pattern.
    4. Render the inline error: below the member search area (after the member selection Input/results dropdown), add:
       `{fineBlockError && <p className="text-sm text-destructive mt-2">{fineBlockError}</p>}`

    Preserve all existing state and behavior — only add fineBlockError state, its reset points, and the conditional paragraph.

    PART B — Modify `src/components/layout/AppSidebar.tsx`:
    Read the full file. Make these two targeted changes:

    Change 1 — LIBRARIAN_NAV additions:
    Add `Receipt` and `ClipboardList` to the lucide-react import block (line ~7). Then add two entries to the LIBRARIAN_NAV array (after the Loans entry):
    - `{ href: "/fines", label: "Fines", icon: Receipt }`
    - `{ href: "/audit", label: "Audit Log", icon: ClipboardList }`

    Final LIBRARIAN_NAV order must be: Dashboard → Books → Members → Loans → Fines → Audit Log.

    Change 2 — MEMBER_NAV My Reservations:
    Find the My Reservations entry in MEMBER_NAV (currently has `disabled: true`). Remove the `disabled: true` property entirely. The entry must become: `{ href: "/my-reservations", label: "My Reservations", icon: BookOpen }`.

    Do not change any other nav items. Do not modify the NavLink component or SidebarContent component.
  </action>
  <verify>
    <automated>cd "C:/Users/ASUS/library-management-system" && npx tsc --noEmit 2>&1 | grep -E "CheckoutSheet|AppSidebar" | wc -l</automated>
  </verify>
  <done>
    - CheckoutSheet renders FINE_BLOCK inline error below member search when checkoutBook returns FINE_BLOCK
    - CheckoutSheet clears inline error when sheet closes or new member is selected
    - AppSidebar LIBRARIAN_NAV includes Fines (Receipt) and Audit Log (ClipboardList) after Loans
    - AppSidebar MEMBER_NAV My Reservations entry has no disabled property
    - TypeScript compilation reports 0 errors in both files
  </done>
  <acceptance_criteria>
    - `grep -c "FINE_BLOCK" src/features/loans/CheckoutSheet.tsx` returns at least 1
    - `grep -c "fineBlockError" src/features/loans/CheckoutSheet.tsx` returns at least 2 (state declaration + render)
    - `grep -c "text-destructive" src/features/loans/CheckoutSheet.tsx` returns 1
    - `grep -c "Receipt" src/components/layout/AppSidebar.tsx` returns at least 2 (import + usage)
    - `grep -c "ClipboardList" src/components/layout/AppSidebar.tsx` returns at least 2 (import + usage)
    - `grep -c '"/fines"' src/components/layout/AppSidebar.tsx` returns 1
    - `grep -c '"/audit"' src/components/layout/AppSidebar.tsx` returns 1
    - `grep -c "disabled.*true" src/components/layout/AppSidebar.tsx` returns 0 (disabled property removed from My Reservations)
    - `npx tsc --noEmit 2>&1 | grep -E "CheckoutSheet|AppSidebar"` returns empty (no TS errors)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → renewLoan | loanId sent by member browser — blocking enforced server-side in Plan 04 |
| Client → CheckoutSheet UI | FINE_BLOCK error message is server-generated — not client-crafted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | Tampering | ActiveLoansClient Renew button | mitigate | renewLoan Server Action enforces MEMBER auth and ownership server-side (Plan 04); UI button is display-only convenience |
| T-03-06-02 | Information Disclosure | FINE_BLOCK error in CheckoutSheet | accept | Fine amount shown to LIBRARIAN who is already authorized to view member records — no privacy concern |
| T-03-06-03 | Spoofing | AppSidebar nav links | accept | Nav links are display-only; actual auth enforcement is in Server Actions and page auth guards — sidebar manipulation doesn't bypass security |
| T-03-06-04 | Elevation of Privilege | MEMBER_NAV My Reservations enabled | mitigate | /my-reservations page has its own auth guard (Plan 03); enabling the nav link only exposes the route link, not the data |
| T-03-06-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
After both tasks complete:
1. `npx tsc --noEmit` — 0 errors in the four modified files
2. `grep "FINE_BLOCK" src/features/loans/CheckoutSheet.tsx` — matches
3. `grep "Receipt" src/components/layout/AppSidebar.tsx` — matches
4. `grep "ClipboardList" src/components/layout/AppSidebar.tsx` — matches
5. `grep "disabled" src/components/layout/AppSidebar.tsx` — returns 0 matches (My Reservations no longer disabled)
6. Manual: Log in as member → /my-loans → active loans show Renew button → click Renew → success toast with new due date
7. Manual: Log in as librarian → sidebar shows "Fines" and "Audit Log" nav items
8. Manual: Log in as member → sidebar shows "My Reservations" as active link (not greyed out)
9. Manual: Checkout for member with unpaid fines above threshold → inline error appears below member search
</verification>

<success_criteria>
- Active loans on /my-loans have Renew button that calls renewLoan and shows specific toast per block type
- CheckoutSheet shows inline FINE_BLOCK error (not toast) when member has excess unpaid fines
- AppSidebar librarian nav includes Fines and Audit Log as clickable links
- AppSidebar member nav has My Reservations as a clickable link (not disabled)
- All Phase 3 user-facing features are now navigable from the sidebar
</success_criteria>

<output>
Create `.planning/phases/03-fines-reservations-renewals-audit/03-06-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

### New Components
- `ActiveLoansClient` — in `src/features/loans/ActiveLoansClient.tsx` — client component for active loans section with Renew button and toast error handling

### Modified Components
- `CheckoutSheet` — in `src/features/loans/CheckoutSheet.tsx` — adds FINE_BLOCK inline error paragraph below member search
- `AppSidebar` — in `src/components/layout/AppSidebar.tsx` — adds Fines + Audit Log to LIBRARIAN_NAV; removes disabled from My Reservations in MEMBER_NAV

### Modified Pages
- `/my-loans` — in `src/app/(app)/my-loans/page.tsx` — delegates active loans rendering to ActiveLoansClient

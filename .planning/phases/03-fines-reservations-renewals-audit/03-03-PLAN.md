---
phase: "03"
plan: "03"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/features/reservations/actions.ts
  - src/features/reservations/MyReservationsClient.tsx
  - src/app/(app)/my-reservations/page.tsx
  - src/components/catalog/BookCard.tsx
autonomous: true
requirements:
  - RES-01
  - RES-03
  - RES-04

must_haves:
  truths:
    - "Member can navigate to /my-reservations and see all their reservations in a flat table"
    - "Table shows Book Title, Status badge, Queue Position, Date Requested, Expires, and Actions columns"
    - "PENDING rows show Cancel button; READY rows show 'Pick up by [date]' in Expires column; expired READY rows show date in red"
    - "Clicking Cancel on a PENDING row opens a confirmation dialog; confirming calls cancelReservation"
    - "cancelReservation sets Reservation.status = CANCELLED for PENDING reservations owned by the authenticated member"
    - "cancelReservation returns FORBIDDEN if session user does not own the reservation"
    - "cancelReservation returns NOT_CANCELLABLE if reservation status is not PENDING"
    - "reserveBook (already in catalog/actions.ts) runs lazy expiry of READY reservations past 48h pickup window before creating a new PENDING reservation"
    - "Queue position 'Position X in queue' label appears on PENDING rows only"
    - "Reserve button on BookCard.tsx is wired to reserveBook Server Action; shown (but disabled) when copies are available, enabled when availableCount === 0 (per D-07)"
  artifacts:
    - path: "src/features/reservations/actions.ts"
      provides: "cancelReservation Server Action"
      exports: ["cancelReservation"]
    - path: "src/features/reservations/MyReservationsClient.tsx"
      provides: "Client component with cancellation dialog and table rendering"
      exports: ["MyReservationsClient"]
    - path: "src/app/(app)/my-reservations/page.tsx"
      provides: "/my-reservations page (member-facing)"
    - path: "src/components/catalog/BookCard.tsx"
      provides: "Reserve button wired to reserveBook; disabled when availableCount > 0 (D-07)"
      contains: "reserveBook"
  key_links:
    - from: "src/app/(app)/my-reservations/page.tsx"
      to: "src/features/reservations/MyReservationsClient.tsx"
      via: "MyReservationsClient component prop"
      pattern: "MyReservationsClient.*reservations="
    - from: "src/features/reservations/MyReservationsClient.tsx"
      to: "src/features/reservations/actions.ts"
      via: "cancelReservation import"
      pattern: "import.*cancelReservation.*from.*reservations/actions"
    - from: "src/features/catalog/actions.ts"
      to: "prisma.reservation"
      via: "lazy expiry in reserveBook before creating new PENDING"
      pattern: "expiredReady\|PICKUP_WINDOW"
    - from: "src/components/catalog/BookCard.tsx"
      to: "src/features/catalog/actions.ts"
      via: "reserveBook import + handleReserve startTransition call"
      pattern: "import.*reserveBook.*from.*catalog/actions"
---

<objective>
Build the reservations vertical slice: `cancelReservation` Server Action + `/my-reservations` page + `MyReservationsClient` component + lazy expiry added to `reserveBook` in catalog/actions.ts. Also verify and document the existing Reserve button wiring in `BookCard.tsx` (RES-01 UI entry point — D-07). After this plan, members can view and cancel their own PENDING reservations, expired READY reservations are cleaned up lazily when a new reservation is placed, and the Reserve button on the catalog card is confirmed end-to-end for the member.

RES-02 (hold-advance on return) was already implemented in Phase 2's `returnBook`. This plan confirms that logic is in place and extends `reserveBook` with lazy expiry.

Purpose: Close RES-01 (reservation placement UI confirmed and documented), RES-03 (lazy expiry), RES-04 (cancellation).
Output: cancelReservation action, MyReservationsClient, /my-reservations page, reserveBook extended with lazy expiry, BookCard.tsx Reserve button wiring verified.
</objective>

<execution_context>
@C:/Users/ASUS/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/ASUS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-fines-reservations-renewals-audit/03-CONTEXT.md
@.planning/phases/03-fines-reservations-renewals-audit/03-UI-SPEC.md
@.planning/phases/03-fines-reservations-renewals-audit/03-PATTERNS.md
@.planning/phases/03-fines-reservations-renewals-audit/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cancelReservation Server Action + reserveBook lazy expiry extension</name>
  <files>
    src/features/reservations/actions.ts
    src/features/catalog/actions.ts
  </files>
  <read_first>
    - src/features/catalog/actions.ts (full file — read reserveBook at lines 209-248; will add lazy expiry inside it; also read session-capture MEMBER auth pattern)
    - src/features/loans/actions.ts (transaction pattern with prisma.$transaction, error handling pattern)
    - src/lib/constants.ts (check if exists; create if not — needed for PICKUP_WINDOW_HOURS)
    - .planning/phases/03-fines-reservations-renewals-audit/03-CONTEXT.md (D-08 reserveBook single-click, D-12 lazy cancellation locations, D-13 expiry-then-advance pattern)
    - .planning/phases/03-fines-reservations-renewals-audit/03-PATTERNS.md (reservations/actions.ts section — cancelReservation full pattern; catalog/actions.ts section — session capture pattern)
  </read_first>
  <behavior>
    cancelReservation:
    - Called with non-existent reservationId → returns { success: false, error: "NOT_FOUND" }
    - Called with reservationId owned by different member → returns { success: false, error: "FORBIDDEN" }
    - Called with reservation in READY/FULFILLED/CANCELLED status → returns { success: false, error: "NOT_CANCELLABLE" }
    - Called with MEMBER session + own PENDING reservation → sets status = "CANCELLED", revalidates /my-reservations, returns { success: true, data: undefined }
    - Called with LIBRARIAN session → returns { success: false, error: "FORBIDDEN" } (requireRole("MEMBER") rejects non-members)

    reserveBook lazy expiry (per D-12):
    - Before creating a new PENDING reservation for a book, cancel any READY reservations for that book where notifiedAt + 48h < now()
    - After expiring READY reservations, advance the next PENDING reservation to READY if one exists (D-13: set copy status to RESERVED, reservation to READY, notifiedAt = now())
    - If no expired reservations, proceed to existing create-PENDING logic unchanged
  </behavior>
  <action>
    PART A — Create `src/features/reservations/actions.ts`:
    New file. Exports: ActionResult type, cancelReservation function.

    "use server" at top. Imports: requireRole, prisma, revalidatePath.

    cancelReservation(reservationId: string): Promise<ActionResult<void>>:
    1. Auth: `let session; try { session = await requireRole("MEMBER"); } catch (err) { return { success: false, error: ... }; }` — captures session for ownership check.
    2. prisma.$transaction(async (tx) => {
       a. tx.reservation.findUnique({ where: { id: reservationId }, include: { member: { include: { user: true } }, book: true } })
       b. If !reservation → throw new Error("NOT_FOUND")
       c. If reservation.member.userId !== session.user.id → throw new Error("FORBIDDEN")
       d. If reservation.status !== "PENDING" → throw new Error("NOT_CANCELLABLE")
       e. tx.reservation.update({ where: { id: reservationId }, data: { status: "CANCELLED" } })
    })
    3. Catch block: handle "NOT_FOUND", "FORBIDDEN", "NOT_CANCELLABLE" with specific error strings; log + return "DB_ERROR" for others.
    4. revalidatePath("/my-reservations") on success.

    Note: Queue position renumbering is NOT required — positions are ordinal display values, not tightly packed; a gap after cancellation is acceptable for v1.

    PART B — Extend `src/features/catalog/actions.ts` — add lazy expiry inside reserveBook:
    Read the full reserveBook function (lines 209-248). Add the following logic BEFORE the existing `await prisma.reservation.create(...)` call:

    1. Create (or import from) `src/lib/constants.ts` if it does not exist. The file exports `export const PICKUP_WINDOW_HOURS = 48;`.
    2. Inside reserveBook, after the `if (existing) return { success: false, error: "ALREADY_RESERVED" };` check but before the `prisma.reservation.create`, add lazy expiry logic:
       a. Find all READY reservations for this bookId where notifiedAt + PICKUP_WINDOW_HOURS * 3600 * 1000 < now() (UTC epoch math — no setDate).
       b. For each expired READY reservation: update status to "CANCELLED".
       c. After expiring, find the AVAILABLE copy that was RESERVED for this expired reservation and find the next PENDING reservation (by queuePosition asc, requestedAt asc).
       d. If a next PENDING reservation exists AND there is a RESERVED copy available: advance that reservation to READY (status = "READY", notifiedAt = now()), set that copy status to "RESERVED" (it already is — just set notifiedAt on reservation).

    Important: The existing reserveBook function does NOT use a prisma.$transaction. Wrap the entire reservation creation block (lazy expiry + create PENDING) in a prisma.$transaction to ensure atomicity. The transaction must include: expire READY reservations → advance next PENDING if applicable → create new PENDING reservation with correct queuePosition.

    The existing queuePosition logic (last?.queuePosition + 1) must remain correct after the transaction wrapper.

    Import PICKUP_WINDOW_HOURS from "@/lib/constants" at the top of catalog/actions.ts.
  </action>
  <verify>
    <automated>cd "C:/Users/ASUS/library-management-system" && npx tsc --noEmit 2>&1 | grep -E "reservations/actions|catalog/actions" | wc -l</automated>
  </verify>
  <done>
    - src/features/reservations/actions.ts exists and exports cancelReservation
    - src/lib/constants.ts exists and exports PICKUP_WINDOW_HOURS = 48
    - reserveBook in catalog/actions.ts imports PICKUP_WINDOW_HOURS and runs lazy expiry before creating PENDING reservation
    - TypeScript compilation reports 0 errors in both files
  </done>
  <acceptance_criteria>
    - `grep -c "export async function cancelReservation" src/features/reservations/actions.ts` returns 1
    - `grep -c "NOT_CANCELLABLE" src/features/reservations/actions.ts` returns 1
    - `grep -c "PICKUP_WINDOW_HOURS" src/features/catalog/actions.ts` returns at least 1
    - `grep -c "export const PICKUP_WINDOW_HOURS = 48" src/lib/constants.ts` returns 1
    - `npx tsc --noEmit 2>&1 | grep -E "reservations/actions|catalog/actions"` returns empty (no TS errors)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: MyReservationsClient component + /my-reservations page</name>
  <files>
    src/features/reservations/MyReservationsClient.tsx
    src/app/(app)/my-reservations/page.tsx
  </files>
  <read_first>
    - src/app/(app)/my-loans/page.tsx (analog — member page auth guard pattern, member lookup with include, inline Table structure, empty state pattern)
    - src/features/loans/ReturnModal.tsx (analog — Dialog pattern for cancellation confirmation)
    - src/features/loans/LoansTable.tsx (useTransition + toast pattern, semantic Badge className pattern)
    - .planning/phases/03-fines-reservations-renewals-audit/03-UI-SPEC.md (/my-reservations page spec, Reservation Cancellation Dialog spec, badge colors table, copywriting contract)
    - .planning/phases/03-fines-reservations-renewals-audit/03-PATTERNS.md (my-reservations/page.tsx section — auth pattern, pickup deadline calculation, empty state; Dialog pattern)
  </read_first>
  <action>
    Create two files:

    FILE 1: `src/features/reservations/MyReservationsClient.tsx`
    "use client" component. Accepts props: `reservations` array where each item has: id, bookId, book.title, status (ReservationStatus), queuePosition, requestedAt, notifiedAt, memberId.

    State: dialogOpen boolean, selectedReservation (id + book title for the dialog), isPending from useTransition.

    Table columns (per UI-SPEC D-09):
    1. Book Title — `className="font-semibold"`.
    2. Status — Badge with semantic colors:
       - PENDING: `bg-yellow-100 text-yellow-800 hover:bg-yellow-100`
       - READY: `bg-green-100 text-green-800 hover:bg-green-100`
       - FULFILLED: `bg-gray-100 text-gray-600 hover:bg-gray-100`
       - CANCELLED: `bg-red-100 text-red-800 hover:bg-red-100`
    3. Queue Position — PENDING rows: "Position X in queue" (text-sm text-muted-foreground); other statuses: "—".
    4. Date Requested — `new Date(requestedAt).toLocaleDateString("en-US", { month: "short", day: "numeric", year: "numeric" })` — text-sm text-muted-foreground.
    5. Expires — READY rows only: compute `pickupDeadline = new Date(notifiedAt.getTime() + 48 * 60 * 60 * 1000)` (UTC epoch math — no setDate); check `isExpired = pickupDeadline < new Date()`. If not expired: "Pick up by [date]" in `className="text-green-800"`. If expired: same text but `className="text-red-600 font-semibold"`. Other statuses: "—".
    6. Actions — PENDING rows: `<Button size="sm" variant="outline" onClick={() => openDialog(reservation)}>Cancel</Button>`. Other statuses: "—".

    Cancellation Dialog (per UI-SPEC D-10):
    - DialogTitle: "Cancel Reservation"
    - DialogDescription: "Cancel your reservation for '[Book Title]'? You will lose your place in the queue."
    - DialogFooter: `<Button variant="outline">Keep Reservation</Button>` + `<Button variant="destructive" disabled={isPending}>Yes, Cancel</Button>`
    - On "Yes, Cancel": startTransition with cancelReservation(selectedReservation.id); on success: toast.success("Reservation cancelled."), close dialog; on error: toast.error("Couldn't cancel reservation. Please try again.").

    Empty state: single TableRow with `<TableCell colSpan={6} className="text-center text-muted-foreground py-8">` containing heading "No reservations" and body "When a book you want is unavailable, click Reserve on the catalog page to join the queue."

    FILE 2: `src/app/(app)/my-reservations/page.tsx`
    Server Component. Follow my-loans/page.tsx pattern:
    - Import auth, headers, redirect, prisma, MyReservationsClient.
    - Auth guard: session check (no role check — any authenticated user with a member record).
    - Member lookup: prisma.member.findUnique({ where: { userId: session.user.id }, include: { reservations: { include: { book: true }, orderBy: { requestedAt: "desc" } } } })
    - If !member: render "No member record found for your account."
    - Pass reservations to MyReservationsClient.
    - Page JSX: `<div className="space-y-6">`, h1 "My Reservations" (text-2xl font-semibold), span "({total} reservations)" (text-sm text-muted-foreground), MyReservationsClient.

    Note: Sidebar activation of My Reservations (remove disabled: true) is done in Plan 06 — do not modify AppSidebar.tsx in this plan.
  </action>
  <verify>
    <automated>cd "C:/Users/ASUS/library-management-system" && npx tsc --noEmit 2>&1 | grep -E "MyReservationsClient|my-reservations/page" | wc -l</automated>
  </verify>
  <done>
    - src/features/reservations/MyReservationsClient.tsx exists with dialog and table
    - src/app/(app)/my-reservations/page.tsx exists with member auth guard and data fetch
    - TypeScript compilation reports 0 errors in both files
    - /my-reservations route accessible to authenticated members
  </done>
  <acceptance_criteria>
    - `grep -c "export function MyReservationsClient" src/features/reservations/MyReservationsClient.tsx` returns 1
    - `grep -c "Cancel Reservation" src/features/reservations/MyReservationsClient.tsx` returns 1
    - `grep -c "PICKUP_WINDOW_HOURS\|48 \* 60 \* 60 \* 1000" src/features/reservations/MyReservationsClient.tsx` returns at least 1
    - `grep -c "Position.*in queue" src/features/reservations/MyReservationsClient.tsx` returns 1
    - `grep -c "MyReservationsClient" src/app/(app)/my-reservations/page.tsx` returns 1
    - `grep -c "userId: session.user.id" src/app/(app)/my-reservations/page.tsx` returns 1
    - `npx tsc --noEmit 2>&1 | grep -E "MyReservationsClient|my-reservations/page"` returns empty (no TS errors)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Verify BookCard.tsx Reserve button wiring for RES-01 (D-07)</name>
  <files>
    src/components/catalog/BookCard.tsx
  </files>
  <read_first>
    - src/components/catalog/BookCard.tsx (full file — confirm reserveBook import, handleReserve startTransition call, disabled={book.availableCount > 0 || isPending} condition, and all three toast messages)
    - src/features/catalog/actions.ts (lines 209-248 — confirm reserveBook export signature matches the import in BookCard.tsx)
    - .planning/phases/03-fines-reservations-renewals-audit/03-CONTEXT.md (D-07: Reserve button on BookCard.tsx when availableCopies === 0)
    - .planning/phases/03-fines-reservations-renewals-audit/03-UI-SPEC.md (BookCard.tsx Reserve Button section — current state and toast copy)
  </read_first>
  <action>
    This task is a wiring verification, not a new implementation. BookCard.tsx already has the Reserve button wired to the reserveBook Server Action (per UI-SPEC: "Button is already present and functional in codebase").

    Read `src/components/catalog/BookCard.tsx` in full. Verify all of the following are present and correct per D-07:

    1. `import { reserveBook } from "@/features/catalog/actions"` — Server Action is imported directly (not a local wrapper).
    2. `const [isPending, startTransition] = useTransition()` — transition state for loading.
    3. `handleReserve` function calls `startTransition(async () => { const result = await reserveBook(book.id); ... })`.
    4. Toast messages match UI-SPEC copywriting contract exactly:
       - result.success → `toast.success("You're on the waitlist for this book.")`
       - result.error === "ALREADY_RESERVED" → `toast.info("You already have a reservation for this book.")`
       - fallthrough → `toast.error("Couldn't place reservation. Please try again.")`
    5. Reserve button has `disabled={book.availableCount > 0 || isPending}` — active only when 0 copies available (D-07).
    6. Reserve button calls `onClick={handleReserve}`.

    If ALL six conditions are met: make no code changes. Add an inline comment at the top of BookCard.tsx (after the existing imports, before the component) reading:
    `// RES-01 (D-07): Reserve button wired to reserveBook — verified Phase 3 Plan 03`
    This comment serves as the traceability marker so the checker can confirm coverage without re-reading the full source.

    If any condition is NOT met: fix the gap (e.g., wrong toast copy, missing disabled condition, missing startTransition) to match the spec above. Do not change the visual layout or card structure.

    Do NOT change: card layout, badge logic, Link wrapper, any import other than what is needed to satisfy the six conditions above.
  </action>
  <verify>
    <automated>cd "C:/Users/ASUS/library-management-system" && grep -c "reserveBook" src/components/catalog/BookCard.tsx</automated>
  </verify>
  <done>
    - `src/components/catalog/BookCard.tsx` imports and calls `reserveBook` from `@/features/catalog/actions`
    - Reserve button `disabled` condition is `book.availableCount > 0 || isPending` (active only when 0 copies)
    - All three toast messages match UI-SPEC copywriting contract exactly
    - RES-01 (D-07) traceability comment present in file
    - TypeScript reports 0 errors in BookCard.tsx
  </done>
  <acceptance_criteria>
    - `grep -c "import.*reserveBook.*from.*catalog/actions" src/components/catalog/BookCard.tsx` returns 1
    - `grep -c "reserveBook(book.id)" src/components/catalog/BookCard.tsx` returns 1
    - `grep -c "availableCount > 0" src/components/catalog/BookCard.tsx` returns 1
    - `grep -c "You're on the waitlist" src/components/catalog/BookCard.tsx` returns 1
    - `grep -c "RES-01" src/components/catalog/BookCard.tsx` returns 1
    - `npx tsc --noEmit 2>&1 | grep "BookCard"` returns empty (no TS errors)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → cancelReservation | reservationId submitted by member browser |
| Server Action → DB | cancelReservation reads member.userId to enforce ownership before mutation |
| Client → reserveBook (BookCard) | bookId submitted by member browser; Server Action enforces MEMBER auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Spoofing | cancelReservation | mitigate | requireRole("MEMBER") verifies session server-side; LIBRARIAN sessions are rejected |
| T-03-03-02 | Elevation of Privilege | cancelReservation — IDOR | mitigate | Ownership check: reservation.member.userId !== session.user.id → FORBIDDEN; prevents member A from cancelling member B's reservation |
| T-03-03-03 | Tampering | reserveBook lazy expiry | mitigate | Lazy expiry runs inside prisma.$transaction — expiry + PENDING advance + new reservation are atomic; no partial state possible |
| T-03-03-04 | Information Disclosure | /my-reservations | mitigate | Page filters by userId: session.user.id — members see only their own reservations |
| T-03-03-05 | Tampering | queuePosition on cancelReservation | accept | Gaps in queue position after cancellation are acceptable for v1 (noted in action); no integrity risk |
| T-03-03-06 | Spoofing | reserveBook via BookCard | mitigate | reserveBook calls requireRole("MEMBER") server-side; bookId originates from rendered book data, not user-typed input |
| T-03-03-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
After all three tasks complete:
1. `npx tsc --noEmit` — 0 errors in reservations/ and my-reservations/ files
2. `grep "export async function cancelReservation" src/features/reservations/actions.ts` — matches
3. `grep "PICKUP_WINDOW_HOURS" src/features/catalog/actions.ts` — matches
4. `grep "export function MyReservationsClient" src/features/reservations/MyReservationsClient.tsx` — matches
5. `grep "userId: session.user.id" src/app/(app)/my-reservations/page.tsx` — matches
6. `grep "import.*reserveBook.*from.*catalog/actions" src/components/catalog/BookCard.tsx` — matches
7. `grep "RES-01" src/components/catalog/BookCard.tsx` — matches (traceability comment)
8. Manual: Member visits catalog — Reserve button is visible on 0-copy books; clicking it shows "You're on the waitlist for this book." toast
9. Manual: Member visits /my-reservations — sees reservation list; Cancel on PENDING row opens dialog; confirming cancels the reservation
</verification>

<success_criteria>
- cancelReservation enforces MEMBER auth and ownership (IDOR protection)
- Expired READY reservations are lazily cancelled inside reserveBook transaction
- /my-reservations shows all reservations with correct badge colors and cancel capability on PENDING rows
- READY rows show "Pick up by [date]"; expired READY rows show date in red
- RES-01 (reservation placement) confirmed end-to-end: BookCard.tsx Reserve button calls reserveBook Server Action when availableCount === 0 (D-07)
- RES-03 (lazy expiry) and RES-04 (cancellation) implemented in this plan
</success_criteria>

<output>
Create `.planning/phases/03-fines-reservations-renewals-audit/03-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

### New Server Actions
- `cancelReservation(reservationId: string): Promise<ActionResult<void>>` — in `src/features/reservations/actions.ts`

### Modified Server Actions
- `reserveBook(bookId: string)` — in `src/features/catalog/actions.ts` — extended with lazy expiry of READY reservations before creating new PENDING

### New Components
- `MyReservationsClient` — in `src/features/reservations/MyReservationsClient.tsx` — accepts reservations array with book include

### New Routes
- `GET /my-reservations` — `src/app/(app)/my-reservations/page.tsx` — member-facing

### New Files
- `src/lib/constants.ts` — exports `PICKUP_WINDOW_HOURS = 48`

### New Types
- `ActionResult<T>` — exported from `src/features/reservations/actions.ts`

### Verified (no code change expected)
- `src/components/catalog/BookCard.tsx` — Reserve button wired to reserveBook; RES-01 traceability comment added (D-07)
</output>

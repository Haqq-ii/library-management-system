---
phase: "03"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
autonomous: true
requirements:
  - FINE-01
  - FINE-02
  - FINE-03
  - RES-01
  - RES-02
  - RES-03
  - RES-04
  - RNW-01
  - RNW-02
  - RNW-03
  - RNW-04
  - AUD-01
  - AUD-02

must_haves:
  truths:
    - "prisma/schema.prisma contains model AuditLog with id, actorId, action, entityType, entityId, details, createdAt fields"
    - "prisma/schema.prisma contains enum AuditAction with all 9 values: CHECKOUT RETURN FINE_WAIVED BOOK_ADDED BOOK_EDITED BOOK_DELETED MEMBER_ADDED MEMBER_EDITED MEMBER_DEACTIVATED"
    - "Fine model in schema.prisma contains waivedReason String? field"
    - "User model contains auditLogs AuditLog[] relation"
    - "npx prisma migrate status shows all migrations applied with no pending migrations"
    - "npx prisma generate completes without errors"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "AuditLog model + AuditAction enum + waivedReason field"
      contains: "model AuditLog"
  key_links:
    - from: "AuditLog.actorId"
      to: "User.id"
      via: "actor User @relation"
      pattern: "actor.*User.*@relation"
---

<objective>
Extend the Prisma schema with the AuditLog model, AuditAction enum, and waivedReason field on Fine, then run the migration and regenerate the Prisma client. This is the mandatory Wave 1 dependency for all other Phase 3 plans — no plan can write to AuditLog or waive fines until this migration is applied.

Purpose: All downstream plans in Phase 3 need the AuditLog table and Fine.waivedReason column to exist before they can write audit entries or record waiver reasons.
Output: Updated schema.prisma, applied migration, regenerated Prisma client with AuditLog model available.
</objective>

<execution_context>
@C:/Users/ASUS/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/ASUS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-fines-reservations-renewals-audit/03-CONTEXT.md
@.planning/phases/03-fines-reservations-renewals-audit/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend prisma/schema.prisma — AuditLog model, AuditAction enum, Fine.waivedReason</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma (read full file — understand current model order, existing enums, User model relations, Fine model fields)
    - .planning/phases/03-fines-reservations-renewals-audit/03-CONTEXT.md (D-18 — exact AuditLog model definition and AuditAction enum values)
    - .planning/phases/03-fines-reservations-renewals-audit/03-PATTERNS.md (Schema section — exact field placement within Fine model, AuditLog model, User auditLogs relation)
  </read_first>
  <action>
    Make three additive changes to prisma/schema.prisma:

    1. Add `waivedReason String?` to the Fine model. Insert it after the existing `waivedBy String?` field (currently line ~190). Keep all existing Fine fields unchanged.

    2. Add the AuditAction enum after the ReservationStatus enum. The enum must contain exactly these 9 values in order: CHECKOUT, RETURN, FINE_WAIVED, BOOK_ADDED, BOOK_EDITED, BOOK_DELETED, MEMBER_ADDED, MEMBER_EDITED, MEMBER_DEACTIVATED.

    3. Add the AuditLog model after the NotificationLog model (end of file). Fields:
       - id String @id @default(cuid())
       - actorId String
       - action AuditAction
       - entityType String
       - entityId String
       - details Json?
       - createdAt DateTime @default(now()) @db.Timestamptz(3)
       - actor User @relation(fields: [actorId], references: [id])

    4. Add `auditLogs AuditLog[]` relation to the User model, after the existing `member Member?` line.

    Use @db.Timestamptz(3) for the createdAt field (consistent with all other DateTime fields in the schema — established UTC convention from STATE.md). Do not alter any existing model definitions other than the two additions above (waivedReason to Fine, auditLogs to User).
  </action>
  <verify>
    <automated>cd "C:/Users/ASUS/library-management-system" && npx prisma validate</automated>
  </verify>
  <done>
    - prisma/schema.prisma passes `npx prisma validate` with no errors
    - Fine model contains `waivedReason String?` after `waivedBy String?`
    - AuditAction enum exists with all 9 values
    - AuditLog model exists with all 7 fields including @db.Timestamptz(3) on createdAt
    - User model contains `auditLogs AuditLog[]`
  </done>
  <acceptance_criteria>
    - `grep -c "waivedReason" prisma/schema.prisma` returns 1
    - `grep -c "model AuditLog" prisma/schema.prisma` returns 1
    - `grep -c "enum AuditAction" prisma/schema.prisma` returns 1
    - `grep -c "FINE_WAIVED" prisma/schema.prisma` returns 1
    - `grep -c "auditLogs.*AuditLog" prisma/schema.prisma` returns 1
    - `npx prisma validate` exits with code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Run migration and regenerate Prisma client</name>
  <files>prisma/migrations/</files>
  <read_first>
    - prisma/schema.prisma (verify Task 1 changes are present before running migration)
  </read_first>
  <action>
    Run the migration to apply the schema changes to the database, then regenerate the Prisma client.

    Step 1: Run migration (interactive prompt expected — name the migration `phase3-audit-fines`):
    `npx prisma migrate dev --name phase3-audit-fines`

    If the interactive prompt cannot be answered non-interactively in the current environment, use `--create-only` first to generate the migration file, then apply it:
    - `npx prisma migrate dev --create-only --name phase3-audit-fines`
    - `npx prisma migrate deploy`

    Step 2: Regenerate the Prisma client:
    `npx prisma generate`

    The Prisma client output path is `../src/generated/prisma` (per generator block in schema.prisma). After generation, `src/generated/prisma/index.d.ts` must reference AuditLog and AuditAction.

    Do not modify any application code in this task — only run the Prisma CLI commands.
  </action>
  <verify>
    <automated>cd "C:/Users/ASUS/library-management-system" && npx prisma migrate status</automated>
  </verify>
  <done>
    - `npx prisma migrate status` shows all migrations applied, zero pending
    - `npx prisma generate` exits with code 0
    - `src/generated/prisma/index.d.ts` contains `AuditLog` and `AuditAction` type definitions
  </done>
  <acceptance_criteria>
    - `npx prisma migrate status` output contains "All migrations have been applied" (no pending migrations listed)
    - `npx prisma generate` exits with code 0 and no errors printed to stderr
    - `grep -l "AuditLog" src/generated/prisma/index.d.ts` returns the file path (AuditLog type exists in generated client)
    - `grep -l "AuditAction" src/generated/prisma/index.d.ts` returns the file path (AuditAction enum exists)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer CLI → DB | Migration commands run with direct DB access — no application-level auth checks apply here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | prisma/schema.prisma | mitigate | Schema changes reviewed before migration; `npx prisma validate` gate prevents malformed schema from reaching DB |
| T-03-01-02 | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan — only Prisma CLI already in devDependencies |
| T-03-01-03 | Elevation of Privilege | AuditLog.actorId | mitigate | actorId is populated from `session.user.id` captured inside `requireRole()` — cannot be supplied by client; enforced in all plans that write AuditLog |
</threat_model>

<verification>
After both tasks complete:
1. `npx prisma validate` — exits 0
2. `npx prisma migrate status` — shows "All migrations have been applied"
3. `grep "model AuditLog" prisma/schema.prisma` — matches
4. `grep "waivedReason" prisma/schema.prisma` — matches
5. `grep "AuditLog" src/generated/prisma/index.d.ts` — matches
</verification>

<success_criteria>
- AuditLog model and AuditAction enum are present in schema.prisma
- Fine.waivedReason String? field is present in schema.prisma
- Migration has been applied to the local PostgreSQL database
- Prisma client has been regenerated and includes AuditLog types
- All downstream Phase 3 plans can import and use `prisma.auditLog` without additional migration steps
</success_criteria>

<output>
Create `.planning/phases/03-fines-reservations-renewals-audit/03-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

### New Models
- `AuditLog` — Prisma model (table: AuditLog) with fields: id, actorId, action, entityType, entityId, details, createdAt

### New Enums
- `AuditAction` — values: CHECKOUT, RETURN, FINE_WAIVED, BOOK_ADDED, BOOK_EDITED, BOOK_DELETED, MEMBER_ADDED, MEMBER_EDITED, MEMBER_DEACTIVATED

### Modified Models
- `Fine` — new field: `waivedReason String?`
- `User` — new relation: `auditLogs AuditLog[]`

### New Migration
- `prisma/migrations/[timestamp]_phase3_audit_fines/migration.sql`

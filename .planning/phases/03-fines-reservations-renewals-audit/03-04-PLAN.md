---
phase: "03"
plan: "04"
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-03"
files_modified:
  - src/features/loans/actions.ts
autonomous: true
requirements:
  - FINE-03
  - RES-02
  - RNW-01
  - RNW-02
  - RNW-03
  - RNW-04

must_haves:
  truths:
    - "checkoutBook returns { success: false, error: 'FINE_BLOCK:X.XX' } when member has unpaid fines >= maxUnpaidFineAmount"
    - "returnBook cancels expired READY reservations (notifiedAt + 48h < now) before advancing the hold queue"
    - "returnBook advances the next PENDING reservation to READY after expiring stale READY entries"
    - "renewLoan returns { success: true, data: { newDueAt } } when all three block checks pass"
    - "renewLoan returns { success: false, error: 'FINE_BLOCK:X.XX' } when unpaid fines >= maxUnpaidFineAmount (checked first)"
    - "renewLoan returns { success: false, error: 'MAX_RENEWALS:N' } when loan.renewCount >= policy.maxRenewals"
    - "renewLoan returns { success: false, error: 'RESERVATION_BLOCK' } when PENDING or READY reservation exists for the book"
    - "renewLoan updates Loan.dueAt = old dueAt + loanDays * 24h (UTC epoch math) and increments Loan.renewCount"
    - "renewLoan blocks with FORBIDDEN when loan is owned by a different member"
  artifacts:
    - path: "src/features/loans/actions.ts"
      provides: "checkoutBook (FINE_BLOCK), returnBook (lazy expiry), renewLoan (all blocks)"
      exports: ["checkoutBook", "returnBook", "renewLoan"]
  key_links:
    - from: "renewLoan"
      to: "LoanPolicy.maxRenewals"
      via: "prisma.loanPolicy.findUnique({ where: { memberType } })"
      pattern: "loanPolicy.findUnique"
    - from: "checkoutBook"
      to: "Fine.status = UNPAID"
      via: "fine.aggregate _sum amount"
      pattern: "fine.aggregate"
    - from: "returnBook"
      to: "Reservation.notifiedAt"
      via: "lazy expiry: notifiedAt + PICKUP_WINDOW_HOURS * 3600 * 1000 < now"
      pattern: "PICKUP_WINDOW\|expiredReady"
---

<objective>
Extend `src/features/loans/actions.ts` with three targeted changes: (1) FINE_BLOCK enforcement in `checkoutBook`, (2) lazy expiry of READY reservations in `returnBook` before hold-advance, and (3) new `renewLoan` function with all three blocking rules. This is a single-file plan to prevent merge conflicts with Plans 02 and 03.

Purpose: Close FINE-03 (FINE_BLOCK in checkout), RES-02 (hold-advance already in returnBook — extend with lazy expiry), and RNW-01–04 (full renewal with all blocks).
Output: Modified loans/actions.ts with FINE_BLOCK, lazy expiry, and renewLoan.
</objective>

<execution_context>
@C:/Users/ASUS/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/ASUS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-fines-reservations-renewals-audit/03-CONTEXT.md
@.planning/phases/03-fines-reservations-renewals-audit/03-PATTERNS.md
@.planning/phases/03-fines-reservations-renewals-audit/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend checkoutBook with FINE_BLOCK + extend returnBook with lazy expiry + add renewLoan</name>
  <files>src/features/loans/actions.ts</files>
  <read_first>
    - src/features/loans/actions.ts (full file — read entire file to understand current structure of checkoutBook and returnBook before modifying; understand the transaction callback type annotation pattern)
    - src/lib/constants.ts (read PICKUP_WINDOW_HOURS value — created in Plan 03; import this constant)
    - .planning/phases/03-fines-reservations-renewals-audit/03-CONTEXT.md (D-06 FINE_BLOCK threshold source, D-12 lazy expiry locations, D-13 expiry-then-advance pattern, D-14 renewLoan new due date formula, D-15 blocking order, D-16 error codes, D-17 renewCount increment)
    - .planning/phases/03-fines-reservations-renewals-audit/03-PATTERNS.md (loans/actions.ts section — FINE_BLOCK pattern for checkoutBook, lazy expiry pattern for returnBook, full renewLoan implementation pattern)
  </read_first>
  <behavior>
    checkoutBook FINE_BLOCK:
    - Member with SUM(Fine.amount WHERE status = UNPAID) = 5.00 and maxUnpaidFineAmount = 10.00 → checkout proceeds normally
    - Member with SUM(Fine.amount WHERE status = UNPAID) = 10.00 and maxUnpaidFineAmount = 10.00 → returns { success: false, error: "FINE_BLOCK:10.00" }
    - Member with SUM(Fine.amount WHERE status = UNPAID) = 0 → checkout proceeds normally

    returnBook lazy expiry:
    - Book has READY reservation with notifiedAt = 72 hours ago (past 48h window) → reservation status set to CANCELLED
    - After cancelling expired READY, next PENDING reservation advances to READY + copy set to RESERVED
    - Book has READY reservation with notifiedAt = 12 hours ago (within window) → reservation NOT cancelled, proceeds as before

    renewLoan blocking order (D-15 — checked in this exact sequence):
    - Block 1 checked first: FINE_BLOCK:X.XX if unpaid fines >= maxUnpaidFineAmount
    - Block 2 checked second: MAX_RENEWALS:N if renewCount >= maxRenewals
    - Block 3 checked third: RESERVATION_BLOCK if active PENDING or READY reservation exists for book

    renewLoan success:
    - new dueAt = current loan.dueAt + policy.loanDays * 24 * 60 * 60 * 1000 (epoch math)
    - Loan.renewCount incremented by 1 (not reset — additive)
    - Loan.status NOT changed (stays ACTIVE or OVERDUE — renewal doesn't clear overdue)
    - Returns { success: true, data: { newDueAt: Date } }
    - revalidatePath("/my-loans") called on success

    renewLoan ownership:
    - Member A cannot renew Member B's loan → returns { success: false, error: "FORBIDDEN" }
  </behavior>
  <action>
    Modify `src/features/loans/actions.ts` with three targeted changes. Read the full file first to understand its current structure.

    CHANGE 1 — Add FINE_BLOCK check in checkoutBook:
    In checkoutBook, after the policy lookup (after `if (!policy) return { success: false, error: "NO_POLICY" };`) and BEFORE the prisma.$transaction call, add:
    - `const unpaidFines = await prisma.fine.aggregate({ where: { memberId, status: "UNPAID" }, _sum: { amount: true } });`
    - `const unpaidTotal = Number(unpaidFines._sum.amount ?? 0);`
    - `if (unpaidTotal >= Number(policy.maxUnpaidFineAmount)) { return { success: false, error: \`FINE_BLOCK:${unpaidTotal.toFixed(2)}\` }; }`

    This check runs outside the transaction (read-only fine check; no concurrency risk — fine amounts only change via librarian action) but BEFORE acquiring the copy lock.

    CHANGE 2 — Add lazy expiry in returnBook:
    Import PICKUP_WINDOW_HOURS from "@/lib/constants" at the top of the file.

    Inside the returnBook prisma.$transaction callback, BEFORE the existing `pendingReservation = await tx.reservation.findFirst(...)` call:
    - `const PICKUP_WINDOW_MS = PICKUP_WINDOW_HOURS * 60 * 60 * 1000;`
    - `const now_expiry = new Date();`
    - Find expired READY reservations: `tx.reservation.findMany({ where: { bookId, status: "READY", notifiedAt: { lt: new Date(now.getTime() - PICKUP_WINDOW_MS) } } })`
    - For each expired: `tx.reservation.update({ where: { id: expired.id }, data: { status: "CANCELLED" } })`
    - Then proceed to the existing `pendingReservation = await tx.reservation.findFirst(...)` logic unchanged.

    Note: The `now` variable already exists in the transaction callback for the overdue check. Reuse it for the expiry check (pass the same `now` timestamp into the expiry calculation for consistency).

    CHANGE 3 — Add renewLoan function at end of file:
    Add `export async function renewLoan(loanId: string): Promise<ActionResult<{ newDueAt: Date }>>` following the full pattern from PATTERNS.md:

    1. Auth: `let session; try { session = await requireRole("MEMBER"); } catch { return FORBIDDEN; }` — captures session for ownership check.
    2. prisma.$transaction(async (tx) => {
       a. tx.loan.findUnique({ where: { id: loanId }, include: { member: { include: { user: true } }, copy: { include: { book: true } } } })
       b. If !loan → throw "NOT_FOUND"
       c. If loan.member.userId !== session.user.id → throw "FORBIDDEN"
       d. tx.loanPolicy.findUnique({ where: { memberType: loan.member.memberType } })
       e. If !policy → throw "NO_POLICY"
       f. Block 1: tx.fine.aggregate({ where: { memberId: loan.memberId, status: "UNPAID" }, _sum: { amount: true } }) → if unpaidTotal >= maxUnpaidFineAmount → throw \`FINE_BLOCK:${unpaidTotal.toFixed(2)}\`
       g. Block 2: if loan.renewCount >= policy.maxRenewals → throw \`MAX_RENEWALS:${policy.maxRenewals}\`
       h. Block 3: tx.reservation.findFirst({ where: { bookId: loan.copy.bookId, status: { in: ["PENDING", "READY"] } } }) → if found → throw "RESERVATION_BLOCK"
       i. Compute newDueAt = new Date(new Date(loan.dueAt).getTime() + policy.loanDays * 24 * 60 * 60 * 1000) — UTC epoch math
       j. tx.loan.update({ where: { id: loanId }, data: { dueAt: newDueAt, renewCount: { increment: 1 } } })
       k. return { newDueAt }
    })
    3. Catch block: if err.message starts with "FINE_BLOCK:" → return that error string; "MAX_RENEWALS:" → return that; "RESERVATION_BLOCK" → return it; "NOT_FOUND" → return it; "FORBIDDEN" → return it; else log + return "DB_ERROR".
    4. revalidatePath("/my-loans") on success.
    5. Return { success: true, data: result }.

    The transaction type annotation for renewLoan should follow the same pattern already in the file — if the existing callbacks use `as unknown as Parameters<...>[0]` casts, apply the same pattern. Alternatively, type the tx parameter as `typeof prisma` if that is how the codebase handles it. Read the existing transaction callbacks to match.
  </action>
  <verify>
    <automated>cd "C:/Users/ASUS/library-management-system" && npx tsc --noEmit 2>&1 | grep "loans/actions" | wc -l</automated>
  </verify>
  <done>
    - src/features/loans/actions.ts exports checkoutBook, returnBook, and renewLoan
    - FINE_BLOCK check present in checkoutBook before the transaction
    - Lazy expiry block present in returnBook before the pendingReservation findFirst
    - renewLoan function present with all three blocking checks in correct order
    - TypeScript compilation reports 0 errors in loans/actions.ts
    - PICKUP_WINDOW_HOURS imported from @/lib/constants
  </done>
  <acceptance_criteria>
    - `grep -c "export async function renewLoan" src/features/loans/actions.ts` returns 1
    - `grep -c "FINE_BLOCK" src/features/loans/actions.ts` returns at least 2 (checkoutBook + renewLoan)
    - `grep -c "MAX_RENEWALS" src/features/loans/actions.ts` returns at least 1
    - `grep -c "RESERVATION_BLOCK" src/features/loans/actions.ts` returns at least 1
    - `grep -c "PICKUP_WINDOW" src/features/loans/actions.ts` returns at least 1
    - `grep -c "PICKUP_WINDOW_HOURS" src/features/loans/actions.ts` returns at least 1
    - `grep -c "from.*constants" src/features/loans/actions.ts` returns 1 (import from lib/constants)
    - `grep -c "renewCount.*increment" src/features/loans/actions.ts` returns 1
    - `npx tsc --noEmit 2>&1 | grep "loans/actions"` returns empty (no TS errors)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → renewLoan | loanId submitted by member browser; all blocking logic evaluated server-side |
| Client → checkoutBook | memberId + bookId submitted by librarian browser; FINE_BLOCK computed server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Spoofing | renewLoan | mitigate | requireRole("MEMBER") verified server-side; loanId ownership validated against session.user.id |
| T-03-04-02 | Elevation of Privilege | renewLoan — IDOR | mitigate | loan.member.userId !== session.user.id check inside transaction before any mutation |
| T-03-04-03 | Tampering | renewLoan blocking rules | mitigate | All three blocks checked server-side in transaction; client cannot bypass by manipulating request |
| T-03-04-04 | Tampering | FINE_BLOCK threshold | mitigate | maxUnpaidFineAmount comes from seeded LoanPolicy DB row — not client-supplied; server-computed aggregate |
| T-03-04-05 | Tampering | Date arithmetic in renewLoan | mitigate | UTC epoch math (getTime() + ms) used throughout — no locale-dependent date operations |
| T-03-04-06 | Tampering | Lazy expiry race condition | mitigate | Expiry runs inside returnBook's prisma.$transaction — atomic with fine creation and hold-advance |
| T-03-04-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
After the task completes:
1. `npx tsc --noEmit` — 0 errors in loans/actions.ts
2. `grep "export async function renewLoan" src/features/loans/actions.ts` — matches
3. `grep "FINE_BLOCK" src/features/loans/actions.ts` — matches (at least 2 occurrences)
4. `grep "PICKUP_WINDOW" src/features/loans/actions.ts` — matches
5. Behavior verification: renewLoan with member having 0 fines, 0 renewals, no reservations → returns { success: true, data: { newDueAt } }
6. Behavior verification: renewLoan with member having unpaid fines >= threshold → returns { success: false, error: "FINE_BLOCK:X.XX" }
</verification>

<success_criteria>
- checkoutBook blocks checkout when unpaid fines >= LoanPolicy.maxUnpaidFineAmount (FINE-03 server-side enforcement)
- returnBook cancels expired READY reservations and advances next PENDING before finding hold candidate
- renewLoan enforces all three blocks in correct order (FINE_BLOCK → MAX_RENEWALS → RESERVATION_BLOCK)
- renewLoan computes new due date with UTC epoch math and increments renewCount
- All changes are in one file to prevent concurrent modification conflicts across plans
</success_criteria>

<output>
Create `.planning/phases/03-fines-reservations-renewals-audit/03-04-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

### Modified Server Actions
- `checkoutBook` — in `src/features/loans/actions.ts` — extended with FINE_BLOCK check before transaction
- `returnBook` — in `src/features/loans/actions.ts` — extended with lazy expiry of READY reservations inside transaction

### New Server Actions
- `renewLoan(loanId: string): Promise<ActionResult<{ newDueAt: Date }>>` — in `src/features/loans/actions.ts` — blocks: FINE_BLOCK, MAX_RENEWALS, RESERVATION_BLOCK; on success: dueAt extended, renewCount incremented

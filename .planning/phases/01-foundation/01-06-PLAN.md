---
phase: 01-foundation
plan: 06
type: execute
wave: 5
depends_on: ["01-05"]
files_modified:
  - src/features/catalog/copy-actions.ts
  - src/features/catalog/CopiesSubTable.tsx
  - src/app/(app)/books/[id]/page.tsx
autonomous: true
requirements: [CAT-02, INFRA-04]
nyquist_compliant: true

must_haves:
  truths:
    - "Clicking a book row opens /books/[id] showing book info and its copies"
    - "Librarian can add a copy; the book's totalCopies increments"
    - "Librarian can mark a copy LOST or WITHDRAWN with confirmation"
    - "Each copy row shows a colored status badge with text label"
  artifacts:
    - path: "src/features/catalog/copy-actions.ts"
      provides: "addCopy + setCopyStatus Server Actions"
      exports: ["addCopy", "setCopyStatus"]
    - path: "src/features/catalog/CopiesSubTable.tsx"
      provides: "Per-book copies sub-table with status badges and actions (D-10)"
      contains: "BookStatusBadge"
    - path: "src/app/(app)/books/[id]/page.tsx"
      provides: "Book detail page with info block + copies sub-table"
  key_links:
    - from: "src/features/catalog/copy-actions.ts"
      to: "src/lib/require-role.ts"
      via: "requireRole LIBRARIAN first line"
      pattern: "requireRole\\(\"LIBRARIAN\"\\)"
    - from: "src/features/catalog/CopiesSubTable.tsx"
      to: "src/components/catalog/BookStatusBadge.tsx"
      via: "status column"
      pattern: "BookStatusBadge"
    - from: "src/app/(app)/books/[id]/page.tsx"
      to: "src/features/catalog/CopiesSubTable.tsx"
      via: "copies render"
      pattern: "CopiesSubTable"
---

<objective>
Build the book detail page (`/books/[id]`) with a book info block and a copies sub-table:
the `copy-actions.ts` Server Actions (`addCopy`, `setCopyStatus`), the `CopiesSubTable`
component using `BookStatusBadge`, and the detail page wiring.

Purpose: Completes physical-copy management (CAT-02) — the second half of the catalog feature
(Wave 5). Builds directly on Plan 05's catalog actions and badge.

Output: Working copy management. Turns the CAT-02 "adding copy increments totalCopies" unit
assertion GREEN (it lives in catalog-actions.test.ts; copy-actions provides the implementation).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@src/lib/require-role.ts
@src/lib/db.ts
@src/components/catalog/BookStatusBadge.tsx
@src/features/catalog/actions.ts
@tests/unit/catalog-actions.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: copy-actions Server Actions (addCopy, setCopyStatus)</name>
  <files>src/features/catalog/copy-actions.ts</files>
  <read_first>
    - .planning/phases/01-foundation/01-PATTERNS.md (CopiesSubTable behaviors; catalog actions Shared Patterns 1-4)
    - .planning/phases/01-foundation/01-UI-SPEC.md (copy confirmations: Mark Lost / Mark Withdrawn copy)
    - tests/unit/catalog-actions.test.ts (the "adding copy increments totalCopies" assertion)
    - src/features/catalog/actions.ts (reuse ActionResult shape + requireRole pattern)
  </read_first>
  <behavior>
    - `addCopy(bookId)` creates a `BookCopy` (status AVAILABLE, unique barcode) and increments the book's `totalCopies` by 1, in a transaction; rejects MEMBER callers
    - `setCopyStatus(copyId, status)` updates a copy to LOST or WITHDRAWN (status from the CopyStatus enum, Zod-validated); rejects MEMBER callers
    - Both return `ActionResult<T>`; neither calls `prisma.bookCopy.delete()`
  </behavior>
  <action>
    `src/features/catalog/copy-actions.ts` ("use server"): import `requireRole`, `prisma`, `z`, `revalidatePath`, and the shared `ActionResult` shape (re-declare or import). `addCopy(bookId: string)`: first line `await requireRole("LIBRARIAN")`; in a `prisma.$transaction`, create a `BookCopy` (status `AVAILABLE`, generate a unique numeric-style `barcode`, e.g. derived from `bookId` + a count) and `prisma.book.update` incrementing `totalCopies` (`{ increment: 1 }`); `revalidatePath("/books/" + bookId)`; return `ActionResult`. `setCopyStatus(copyId, status)`: first line `requireRole("LIBRARIAN")`; Zod-validate `status` against `z.enum(["AVAILABLE","CHECKED_OUT","RESERVED","LOST","WITHDRAWN"])`; `prisma.bookCopy.update({ where: { id: copyId }, data: { status } })`; revalidate the parent book path; return `ActionResult`. No hard deletes — marking WITHDRAWN is a status change, not a row delete (INFRA-04).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/catalog-actions.test.ts --reporter=dot && grep -c "requireRole(\"LIBRARIAN\")" src/features/catalog/copy-actions.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/features/catalog/copy-actions.ts` starts with `"use server"` and exports `addCopy` and `setCopyStatus`
    - First statement of each exported action is `await requireRole("LIBRARIAN")` (count >= 2)
    - `addCopy` uses `prisma.$transaction` and `totalCopies` with `increment: 1`
    - `setCopyStatus` validates status via `z.enum` and does NOT call `prisma.bookCopy.delete(`
    - `npx vitest run tests/unit/catalog-actions.test.ts` exits 0 (CAT-02 totalCopies assertion GREEN)
  </acceptance_criteria>
  <done>Copy actions implemented with requireRole guard, transactional totalCopies increment, enum-validated status changes, no hard deletes; CAT-02 unit assertion GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: CopiesSubTable + book detail page</name>
  <files>src/features/catalog/CopiesSubTable.tsx, src/app/(app)/books/[id]/page.tsx</files>
  <read_first>
    - .planning/phases/01-foundation/01-UI-SPEC.md (Copies Sub-Table contract D-10: columns, Mark Lost/Withdrawn confirmations, Add Copy; book detail surface)
    - .planning/phases/01-foundation/01-PATTERNS.md (CopiesSubTable, book detail page behaviors)
    - src/features/catalog/copy-actions.ts (addCopy, setCopyStatus signatures)
    - src/components/catalog/BookStatusBadge.tsx
  </read_first>
  <action>
    `src/features/catalog/CopiesSubTable.tsx` ("use client"): shadcn `Table` columns Copy ID (barcode), Status (`<BookStatusBadge>`), Added date, Actions; per-row "Mark Lost" / "Mark Withdrawn" each opening a small confirm `Dialog` (UI-SPEC copy: "Mark copy as lost?" / "Mark copy as withdrawn?") whose confirm calls `setCopyStatus(copyId, "LOST"|"WITHDRAWN")`; an "Add Copy" button above the table calling `addCopy(bookId)`; `toast` feedback (Shared Pattern 7). `src/app/(app)/books/[id]/page.tsx` (async Server Component): fetch the book by `params.id` with its `author` and `copies` (`where: { deletedAt: null }` on book); render a `Card` info block (title, author, ISBN, genre, publisher, year, totalCopies) above `<CopiesSubTable bookId={book.id} copies={book.copies} />`; handle not-found (book missing or soft-deleted) with a friendly empty state.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "CopiesSubTable" "src/app/(app)/books/[id]/page.tsx" && grep -q "BookStatusBadge" src/features/catalog/CopiesSubTable.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/features/catalog/CopiesSubTable.tsx` contains `BookStatusBadge`, `Add Copy`, `Mark copy as lost?`, and calls `setCopyStatus` and `addCopy`
    - `src/app/(app)/books/[id]/page.tsx` fetches the book by `params.id` (or awaited params) including `copies` and renders `<CopiesSubTable`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Copies sub-table renders status badges and per-copy actions; book detail page shows info block + copies; Add Copy and status changes wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → copy Server Action | Untrusted caller role + copyId/status |
| action → Prisma transaction | totalCopies consistency surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Elevation of Privilege | copy-actions | mitigate | `requireRole("LIBRARIAN")` first line of `addCopy` and `setCopyStatus` (T-01); not middleware |
| T-06-02 | Tampering | invalid status value | mitigate | `setCopyStatus` Zod-validates status against the `CopyStatus` enum; arbitrary strings rejected (T-02) |
| T-06-03 | Tampering | totalCopies drift | mitigate | `addCopy` wraps copy create + `totalCopies` increment in `prisma.$transaction` for atomicity |
| T-06-04 | Tampering | hard delete of copy | mitigate | WITHDRAWN is a status change, not a row delete; `prisma.bookCopy.delete(` banned (INFRA-04) |
</threat_model>

<verification>
- `npx vitest run tests/unit/catalog-actions.test.ts` passes (CAT-02 totalCopies)
- `npx tsc --noEmit` passes
- `addCopy`/`setCopyStatus` begin with `requireRole("LIBRARIAN")`; no `prisma.bookCopy.delete(`
- `/books/[id]` renders the info block + `CopiesSubTable`; copies show `BookStatusBadge`
</verification>

<success_criteria>
- Book detail page shows book info and a copies sub-table (D-10, CAT-02)
- Adding a copy increments totalCopies atomically; status changes validated against the enum
- Mark Lost / Mark Withdrawn work via confirmation dialogs; no hard deletes (INFRA-04)
- CAT-02 copy unit assertion is GREEN
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-06-SUMMARY.md` when done.
</output>

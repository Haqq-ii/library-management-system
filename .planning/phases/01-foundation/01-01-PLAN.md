---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - components.json
  - src/app/globals.css
  - prisma/schema.prisma
  - prisma/prisma.config.ts
  - .gitignore
  - vitest.config.ts
  - playwright.config.ts
  - tests/unit/require-role.test.ts
  - tests/unit/catalog-actions.test.ts
  - tests/unit/member-actions.test.ts
  - tests/unit/isbn-fetch.test.ts
  - tests/unit/BookStatusBadge.test.tsx
  - tests/unit/seed-verification.test.ts
  - tests/e2e/auth.spec.ts
  - tests/e2e/session.spec.ts
  - tests/e2e/rbac.spec.ts
  - tests/e2e/catalog.spec.ts
  - tests/e2e/member-catalog.spec.ts
  - tests/e2e/members.spec.ts
  - tests/e2e/member-profile.spec.ts
  - prisma/migrations
autonomous: true
requirements: [INFRA-01, INFRA-04]
nyquist_compliant: true

must_haves:
  truths:
    - "Next.js 16 App Router project scaffolds and type-checks"
    - "Prisma schema contains all Phase 1-5 models with Timestamptz columns"
    - "First migration applies cleanly to a Postgres database"
    - "Test harness (Vitest + Playwright) runs and Wave 0 test stubs exist"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Full v1 schema (User/Session/Account/Verification, Member, Author, Book, BookCopy, LoanPolicy + Phase 2-5 stubs)"
      contains: "model BookCopy"
    - path: "prisma/prisma.config.ts"
      provides: "Prisma 7 defineConfig with seed entry"
      contains: "defineConfig"
    - path: "vitest.config.ts"
      provides: "Vitest unit test configuration"
    - path: "playwright.config.ts"
      provides: "Playwright E2E configuration"
    - path: "prisma/migrations"
      provides: "Applied initial migration directory"
  key_links:
    - from: "prisma/schema.prisma"
      to: "src/generated/prisma"
      via: "generator client output"
      pattern: "output\\s*=\\s*\"../src/generated/prisma\""
    - from: "prisma/prisma.config.ts"
      to: "DATABASE_URL"
      via: "env() datasource url"
      pattern: "env\\(\"DATABASE_URL\"\\)"
---

<objective>
Scaffold the greenfield Next.js 16 + Prisma 7 project: create the app, initialize shadcn/ui,
install all Phase 1 dependencies, author the complete v1 Prisma schema (all Phase 1-5 models),
stand up the Vitest + Playwright test harness with Wave 0 test stubs, and apply the first
database migration.

Purpose: This is Wave 1 of the Walking Skeleton. Every downstream file imports the generated
Prisma client and relies on these dependencies. The schema (especially the Book/BookCopy split,
LoanPolicy table, and Timestamptz convention) is a locked foundation that cannot be retrofitted.

Output: A type-checking Next.js project with installed packages, a full Prisma schema, an applied
initial migration, and a green test harness containing the failing Wave 0 test stubs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/SKELETON.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold project, init shadcn/ui, install dependencies</name>
  <files>package.json, tsconfig.json, next.config.ts, components.json, src/app/globals.css, .gitignore</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Standard Stack table, Installation block, shadcn component list)
    - .planning/phases/01-foundation/01-PATTERNS.md (globals.css Tailwind v4 pattern)
    - CLAUDE.md (stack constraints, What NOT to Use)
  </read_first>
  <action>
    Run `npx create-next-app@latest . --typescript --app --tailwind --eslint --src-dir --import-alias "@/*" --use-npm --no-turbopack` in the current empty repo (accept defaults; keep `src/` dir and `@/*` alias). Then run `npx shadcn@latest init` selecting the default (slate/neutral) base color. Install runtime deps with exact versions from RESEARCH.md Standard Stack: `npm install next@16.2.7 react@19.2.7 react-dom@19.2.7 @prisma/client@7.8.0 @prisma/adapter-pg@7.8.0 pg@8.21.0 better-auth@1.6.15 zod@4.4.3 react-hook-form@7.78.0 @hookform/resolvers@5.4.0 lucide-react@1.17.0 sonner@2.0.7`. Install dev deps: `npm install --save-dev prisma@7.8.0 tsx@4.22.4 vitest@4.1.8 @vitejs/plugin-react @testing-library/react@16.3.2 @testing-library/user-event @playwright/test@1.60.0`. Add the shadcn components via `npx shadcn@latest add button input label card badge table sheet dialog form separator avatar dropdown-menu switch skeleton sonner`. Add `"postinstall": "prisma generate"` and seed/test scripts to package.json (`"test": "vitest run"`, `"test:e2e": "playwright test"`, `"db:seed": "tsx prisma/seed.ts"`). Ensure `src/app/globals.css` imports `tailwindcss` and `tw-animate-css` per the PATTERNS.md globals.css pattern (CSS-first; NO `tailwind.config.js`). Append `.env*` to `.gitignore` (keep `.env.example` if present out of ignore is NOT required this phase) so no env files are committed (INFRA-02 prep, INFRA-01).
  </action>
  <verify>
    <automated>npx tsc --noEmit && test -f components.json && test -d src/components/ui && npx eslint --version</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies contains `"@prisma/adapter-pg"` and `"better-auth"` and `"zod"`
    - `package.json` scripts contains `"postinstall": "prisma generate"` and `"db:seed": "tsx prisma/seed.ts"`
    - `components.json` file exists at repo root
    - `src/components/ui/` directory contains `button.tsx`, `table.tsx`, `sheet.tsx`, `sonner.tsx`
    - `src/app/globals.css` contains `@import "tailwindcss"` and `@import "tw-animate-css"`
    - No `tailwind.config.js` file exists (Tailwind v4 CSS-first)
    - `.gitignore` contains `.env`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Project scaffolded; shadcn/ui initialized with 15 components; all packages installed; type-check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Author full Prisma 7 schema and config (auth tables first)</name>
  <files>prisma/schema.prisma, prisma/prisma.config.ts</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Code Examples → Full Prisma Schema; Pattern 4, Pattern 5; Pitfall 1, 2, 5, 7)
    - .planning/phases/01-foundation/01-PATTERNS.md (prisma/schema.prisma and prisma/prisma.config.ts pattern blocks)
  </read_first>
  <action>
    First run `npx auth@latest generate --output prisma/schema.prisma` to scaffold the Better Auth tables (`User`, `Session`, `Account`, `Verification`) BEFORE writing domain models (RESEARCH.md Pitfall 7). Then complete `prisma/schema.prisma` to match the full schema in RESEARCH.md Code Examples verbatim: generator block uses `provider = "prisma-client-js"` (NOT `prisma-client` — Pitfall 1) with `output = "../src/generated/prisma"`; datasource `provider = "postgresql"`, `url = env("DATABASE_URL")`. Models required: `User` (with `deletedAt DateTime? @db.Timestamptz(3)`, admin-plugin fields `role String? @default("MEMBER")`, `banned`, `banReason`, `banExpires`), `Session`, `Account`, `Verification`, enum `MemberType {STUDENT FACULTY STAFF}`, `Member`, `Author`, `Book` (with `deletedAt`, `totalCopies Int @default(0)`), enum `CopyStatus {AVAILABLE CHECKED_OUT RESERVED LOST WITHDRAWN}`, `BookCopy`, `LoanPolicy` (fields `memberType @unique`, `loanDays`, `maxRenewals`, `fineDailyRate Decimal @db.Decimal(10,2)`, `maxUnpaidFineAmount`), and Phase 2-5 stubs: enums `LoanStatus`, `FineStatus`, `ReservationStatus` plus models `Loan`, `Fine`, `Reservation`, `NotificationLog`. EVERY `DateTime` column uses `@db.Timestamptz(3)` (Pattern 4 / STATE.md). Create `prisma/prisma.config.ts` with `import "dotenv/config"` as the FIRST line, then `defineConfig` from `prisma/config` with `schema: "prisma/schema.prisma"`, `migrations: { path: "prisma/migrations", seed: "tsx prisma/seed.ts" }`, and `datasource: { url: env("DATABASE_URL") }`.
  </action>
  <verify>
    <automated>npx prisma validate --schema prisma/schema.prisma</automated>
  </verify>
  <acceptance_criteria>
    - `prisma/schema.prisma` contains `provider = "prisma-client-js"` (grep -v '^//' | grep -c shows >=1) and does NOT contain `provider = "prisma-client"` (without `-js`)
    - `prisma/schema.prisma` contains `output   = "../src/generated/prisma"`
    - `prisma/schema.prisma` contains `model User`, `model Session`, `model Account`, `model Verification`, `model Member`, `model Author`, `model Book`, `model BookCopy`, `model LoanPolicy`, `model Loan`, `model Fine`, `model Reservation`, `model NotificationLog`
    - `prisma/schema.prisma` contains `enum CopyStatus` and `enum MemberType`
    - `prisma/schema.prisma` contains `deletedAt DateTime? @db.Timestamptz(3)` on both User and Book
    - Every `DateTime` line in schema includes `@db.Timestamptz`: `grep -c "DateTime" prisma/schema.prisma` equals count of lines also containing `@db.Timestamptz` (no naive DateTime)
    - `prisma/prisma.config.ts` first non-empty line is `import "dotenv/config";`
    - `prisma/prisma.config.ts` contains `seed: "tsx prisma/seed.ts"`
    - `npx prisma validate` exits 0
  </acceptance_criteria>
  <done>Full v1 schema validates; auth tables present first; Timestamptz on every date column; Prisma 7 config in place.</done>
</task>

<task type="auto">
  <name>Task 3: Test harness config + Wave 0 test stubs</name>
  <files>vitest.config.ts, playwright.config.ts, tests/unit/require-role.test.ts, tests/unit/catalog-actions.test.ts, tests/unit/member-actions.test.ts, tests/unit/isbn-fetch.test.ts, tests/unit/BookStatusBadge.test.tsx, tests/unit/seed-verification.test.ts, tests/e2e/auth.spec.ts, tests/e2e/session.spec.ts, tests/e2e/rbac.spec.ts, tests/e2e/catalog.spec.ts, tests/e2e/member-catalog.spec.ts, tests/e2e/members.spec.ts, tests/e2e/member-profile.spec.ts</files>
  <read_first>
    - .planning/phases/01-foundation/01-VALIDATION.md (Wave 0 Requirements, Per-Task Verification Map)
    - .planning/phases/01-foundation/01-RESEARCH.md (Validation Architecture; test file → requirement map)
    - .planning/phases/01-foundation/01-PATTERNS.md (Vitest unit template, Playwright E2E template)
  </read_first>
  <action>
    Create `vitest.config.ts` using `@vitejs/plugin-react`, `environment: "jsdom"` for the RTL test, `globals: true`, and resolve alias `@` → `./src` so `@/lib/...` imports resolve. Create `playwright.config.ts` with `baseURL: "http://localhost:3000"`, `headless: true`, and the chromium project. Create all 8 unit test files and 7 E2E spec files listed in VALIDATION.md Wave 0 Requirements. Each test file must contain REAL assertions matching the Per-Task Verification Map behaviors (e.g. `require-role.test.ts` asserts `requireRole("LIBRARIAN")` rejects with `"UNAUTHENTICATED"` when no session and `"FORBIDDEN"` on wrong role, mocking `@/lib/auth` and `next/headers` per the PATTERNS.md unit template). Tests that target not-yet-created modules (catalog-actions, member-actions, isbn-fetch, BookStatusBadge, seed-verification) are EXPECTED to fail now (RED) — they are the Nyquist scaffolds Waves 2-5 turn green. Do NOT stub them as `it.skip` or `expect(true)`; write the real intended assertions referencing the future module paths (`@/features/catalog/actions`, `@/features/members/actions`, `@/components/catalog/BookStatusBadge`). E2E specs use the seeded credentials `librarian@library.test` / member account and assert the behaviors in the verification map.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/require-role.test.ts --reporter=dot 2>&1 | head -40; npx playwright test --list 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `vitest.config.ts` contains `jsdom` and an alias mapping `@` to `./src`
    - `playwright.config.ts` contains `baseURL` and `http://localhost:3000`
    - All 8 files under `tests/unit/` exist; all 7 files under `tests/e2e/` exist (15 total)
    - `tests/unit/require-role.test.ts` contains `rejects.toThrow("UNAUTHENTICATED")` and `rejects.toThrow("FORBIDDEN")`
    - `tests/unit/isbn-fetch.test.ts` references `fetchBookByISBN` and asserts `ISBN_NOT_FOUND`
    - `tests/unit/seed-verification.test.ts` asserts seed counts (1 librarian, 10 students, 5 faculty, 20 books)
    - `npx playwright test --list` exits 0 and lists at least 7 specs
    - No test file contains `it.skip(` or `expect(true).toBe(true)` as its only assertion
  </acceptance_criteria>
  <done>Vitest + Playwright configured; all 15 Wave 0 test files exist with real assertions; require-role test can run; Playwright lists specs.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 4: [BLOCKING] Apply initial Prisma migration</name>
  <files>prisma/migrations</files>
  <read_first>
    - prisma/schema.prisma (the schema being migrated)
    - prisma/prisma.config.ts (datasource url + migrations path)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pitfall 2 — explicit generate; Environment Availability — Postgres via Docker)
  </read_first>
  <action>
    [BLOCKING] Schema push is MANDATORY before any code can rely on the generated client. Ensure a Postgres 16 instance is reachable via `DATABASE_URL` (start an ephemeral one if needed: `docker run --rm -d --name lms-migrate-pg -e POSTGRES_DB=library_dev -e POSTGRES_USER=postgres -e POSTGRES_PASSWORD=postgres -p 5432:5432 postgres:16-alpine`, then export `DATABASE_URL=postgresql://postgres:postgres@localhost:5432/library_dev`). Run `npx prisma migrate dev --name init` to create the migration files in `prisma/migrations/` AND apply them. Prisma 7 does NOT auto-generate on migrate (Pitfall 2) — explicitly run `npx prisma generate` afterward so `src/generated/prisma` exists. Verify the generated client imports. Stop the ephemeral container when done (`docker rm -f lms-migrate-pg`). The Docker entrypoint (Plan 03) will run `prisma migrate deploy` in the real dev container; this task only proves the migration is valid and committed.
  </action>
  <verify>
    <automated>test -d prisma/migrations && ls prisma/migrations | grep -q init && test -d src/generated/prisma</automated>
  </verify>
  <acceptance_criteria>
    - `prisma/migrations/` directory exists and contains a subdirectory ending in `_init`
    - The migration subdirectory contains `migration.sql`
    - `migration.sql` contains `CREATE TABLE "User"` and `CREATE TABLE "BookCopy"` and `CREATE TABLE "LoanPolicy"`
    - `src/generated/prisma` directory exists (from `prisma generate`)
    - `npx prisma migrate status` reports the database is up to date (no pending migrations) when run against the migrate DB
  </acceptance_criteria>
  <done>Initial migration generated and applied; CREATE TABLE statements for all Phase 1 tables present; Prisma client generated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host filesystem → git | `.env.*` must never be committed (secrets) |
| npm registry → build | Installed packages execute postinstall scripts |
| developer machine → Postgres | Migration runs DDL against a DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `.env.*` files | mitigate | `.gitignore` includes `.env`; verified by acceptance criterion; no secrets in scaffolded files |
| T-01-02 | Tampering | naive `DateTime` columns | mitigate | Every `DateTime` uses `@db.Timestamptz(3)`; acceptance criterion greps for naive columns (prevents Phase 2-5 overdue/timezone bugs) |
| T-01-03 | Elevation of Privilege | Prisma generator provider | mitigate | Use `prisma-client-js` not `prisma-client` (Pitfall 1) — wrong provider silently breaks runtime auth resolution |
| T-01-SC | Tampering | npm package installs | mitigate | All packages [VERIFIED: npm registry] in RESEARCH.md Package Legitimacy Audit; no [ASSUMED]/[SUS]/[SLOP] packages — install proceeds without legitimacy checkpoint; postinstall scripts checked (none found for better-auth/node-cron/sonner) |
</threat_model>

<verification>
- `npx tsc --noEmit` passes (project type-checks)
- `npx prisma validate` passes (schema is valid)
- `prisma/migrations/*_init/migration.sql` exists with CREATE TABLE for all Phase 1 tables
- `npx playwright test --list` lists the 7 E2E specs
- `npx vitest run tests/unit/require-role.test.ts` runs (will fail until Plan 02 creates require-role.ts — expected RED)
</verification>

<success_criteria>
- Next.js 16 App Router project scaffolds and type-checks
- shadcn/ui initialized with the 15 required components in `src/components/ui/`
- Full v1 Prisma schema validates with auth tables, domain models, Phase 2-5 stubs, and Timestamptz everywhere
- Initial migration generated and applied; Prisma client generated to `src/generated/prisma`
- Test harness configured; all 15 Wave 0 test files exist with real (currently-RED for future modules) assertions
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done.
</output>

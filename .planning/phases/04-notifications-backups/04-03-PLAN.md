---
phase: 04-notifications-backups
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - src/lib/notifications.ts
  - src/features/loans/actions.ts
  - tests/unit/loan-return.test.ts
autonomous: true
requirements: [NOTF-03]

must_haves:
  truths:
    - "When a return advances a pending reservation to READY, the member receives a hold-ready email"
    - "The hold-ready email is sent AFTER the transaction commits, never inside prisma.$transaction"
    - "A return that triggers no hold sends no hold-ready email"
    - "The hold-ready email is sent exactly once per reservation (no date suffix in idempotency key)"
  artifacts:
    - path: "src/features/loans/actions.ts"
      provides: "returnBook extended to return hold member/email/book + post-transaction sendHoldReady call"
      contains: "sendHoldReady"
    - path: "src/lib/notifications.ts"
      provides: "sendHoldReady() wrapper over sendAndLog"
      exports: ["sendHoldReady"]
  key_links:
    - from: "src/features/loans/actions.ts"
      to: "sendHoldReady"
      via: "post-transaction call when holdTriggered is true"
      pattern: "sendHoldReady"
    - from: "src/lib/notifications.ts"
      to: "sendAndLog"
      via: "HOLD_READY send"
      pattern: "sendAndLog"
---

<objective>
Deliver the hold-ready notification vertical slice (NOTF-03): when `returnBook` advances a pending reservation to READY, the member receives a hold-ready email with pickup instructions. The send is event-driven and fires once, after the transaction commits.

Purpose: Closes the reservation loop from Phase 3 — a returned copy that fulfils a hold now actively notifies the waiting member. This is distinct from cron (plan 02): hold-ready is event-driven, never daily.
Output: `sendHoldReady` wrapper, extended `returnBook` return shape + post-commit email call, NOTF-03 test.

Wave note: scheduled to wave 3 (after plan 02) because both plans touch `src/lib/notifications.ts`; sequencing avoids a same-wave file-ownership conflict. Plan 02 creates `notifications.ts` with the cron wrappers; this plan appends `sendHoldReady`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-notifications-backups/04-RESEARCH.md
@.planning/phases/04-notifications-backups/04-PATTERNS.md
@CLAUDE.md

# Consumes plan 01 + plan 02 artifacts
@src/lib/email.ts
@src/lib/notifications.ts
@src/emails/HoldReadyEmail.tsx

# Self — file being modified
@src/features/loans/actions.ts
</context>

<artifacts_this_phase_produces>
- `src/lib/notifications.ts` — `sendHoldReady()` export appended (file created by plan 02)
- `src/features/loans/actions.ts` — extended `returnBook` hold branch + post-transaction `sendHoldReady` call
- `tests/unit/loan-return.test.ts` — NOTF-03 case added to the existing return test file
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sendHoldReady wrapper + extend returnBook hold branch + NOTF-03 test</name>
  <files>src/lib/notifications.ts, src/features/loans/actions.ts, tests/unit/loan-return.test.ts</files>
  <read_first>
    - .planning/phases/04-notifications-backups/04-PATTERNS.md (### src/features/loans/actions.ts returnBook MODIFIED section — required return-shape additions, post-transaction call pattern, anti-pattern: never inside transaction; Pattern 5)
    - .planning/phases/04-notifications-backups/04-RESEARCH.md (Pattern 5, Pitfall 2, Pitfall 6 — HOLD_READY idempotency key has NO date suffix)
    - src/features/loans/actions.ts (returnBook lines ~145-300: current return shape `{ holdTriggered, holdMemberName? }`, the pendingReservation branch lines ~258-273, the post-transaction revalidatePath lines ~286-289)
    - src/lib/email.ts (sendAndLog signature)
    - src/lib/notifications.ts (existing cron wrappers from plan 02 — append, do not rewrite)
    - src/emails/HoldReadyEmail.tsx (props: memberName, bookTitle, pickupWindowHours)
    - src/lib/constants.ts (PICKUP_WINDOW_HOURS)
    - tests/unit/loan-return.test.ts (existing structure to extend)
  </read_first>
  <behavior>
    - Test 1 (NOTF-03 positive): a returnBook call where a PENDING reservation exists for the book causes sendHoldReady to be called once, AFTER the transaction resolves, with the reserving member's id/email/name and bookTitle.
    - Test 2 (NOTF-03 negative): a returnBook call with no pending reservation (holdTriggered false) does NOT call sendHoldReady.
    - Test 3 (idempotency): the idempotencyKey passed to the hold-ready send equals `HOLD_READY/<reservationId>` with no date component.
    - Test 4 (no-throw isolation): if sendHoldReady rejects, returnBook still returns success:true (email failure must not roll back or fail the completed return).
  </behavior>
  <action>In `src/lib/notifications.ts` ADD (append; do not overwrite plan 02's wrappers) `export async function sendHoldReady(opts: { memberId: string; memberEmail: string; memberName: string; bookTitle: string; pickupWindowHours: number; idempotencyKey: string })` that builds `React.createElement(HoldReadyEmail, { memberName, bookTitle, pickupWindowHours })` and calls `sendAndLog` from `@/lib/email` with `to: opts.memberEmail`, subject like "Your reserved book is ready for pickup", `memberId: opts.memberId`, `type: "HOLD_READY"`, and `idempotencyKey: opts.idempotencyKey`. In `src/features/loans/actions.ts` returnBook: (1) extend the transaction's hold branch return object (currently `{ holdTriggered: true, holdMemberName }`) to also include `holdMemberId: pendingReservation.member.id`, `holdMemberEmail: pendingReservation.member.user.email`, `bookTitle: loan.copy.book.title`, and `reservationId: pendingReservation.id`; widen the function's declared return type accordingly. (2) After `const data = await prisma.$transaction(...)` resolves and BEFORE the `revalidatePath` calls, add: if `data.holdTriggered && data.holdMemberEmail`, call `sendHoldReady({ memberId: data.holdMemberId!, memberEmail: data.holdMemberEmail!, memberName: data.holdMemberName ?? "", bookTitle: data.bookTitle ?? "", pickupWindowHours: PICKUP_WINDOW_HOURS, idempotencyKey: \`HOLD_READY/${data.reservationId}\` })` inside a try/catch that logs failures (so a send error does not fail the return). NEVER call sendHoldReady inside the transaction. Add the four NOTF-03 behaviors to `tests/unit/loan-return.test.ts`, mocking `@/lib/notifications` (or `@/lib/email`) to spy on the hold-ready send, and asserting call timing (after transaction), key format, and no-throw isolation.</action>
  <verify>
    <automated>npm test -- tests/unit/loan-return.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- tests/unit/loan-return.test.ts` passes including the four NOTF-03 cases.
    - `src/lib/notifications.ts` exports `sendHoldReady` AND still exports plan 02's `sendDueDateReminder`/`sendOverdueAlert` (no overwrite): `grep -c "export async function" src/lib/notifications.ts` returns >= 3.
    - `returnBook` calls `sendHoldReady` only after `prisma.$transaction(...)` resolves: the `sendHoldReady` line number is greater than the `await prisma.$transaction` line, with no `sendHoldReady` reference between the `$transaction(` open and its matching close.
    - Hold-ready idempotency key contains no `toISOString` / date suffix.
  </acceptance_criteria>
  <done>returnBook sends a hold-ready email exactly once, after commit, when a hold advances; no send when no hold; NOTF-03 tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| returnBook (server action) -> Resend API | post-transaction outbound email |
| transaction boundary | email must NOT cross into the DB transaction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Denial of Service | email inside transaction holds DB connection / rolls back | mitigate | sendHoldReady called only after `$transaction` resolves; verified by grep line-order gate |
| T-04-09 | Denial of Service | duplicate hold-ready emails | mitigate | idempotencyKey `HOLD_READY/<reservationId>` (no date) — once-ever dedup |
| T-04-10 | Tampering | email failure rolls back a completed return | mitigate | sendHoldReady wrapped in try/catch; return success unaffected by send failure |
</threat_model>

<verification>
- `npm test -- tests/unit/loan-return.test.ts` green (NOTF-03).
- Grep gate confirms post-transaction call ordering and date-less idempotency key.
- Full `npm test` suite green.
</verification>

<success_criteria>
- A return that fulfils a hold sends one hold-ready email after commit (NOTF-03).
- A return with no hold sends nothing.
- Email failures never fail the return.
</success_criteria>

<output>
Create `.planning/phases/04-notifications-backups/04-03-SUMMARY.md` when done.
</output>

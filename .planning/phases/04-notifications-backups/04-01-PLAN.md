---
phase: 04-notifications-backups
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - package.json
  - src/lib/email.ts
  - src/emails/DueDateReminderEmail.tsx
  - src/emails/OverdueAlertEmail.tsx
  - src/emails/HoldReadyEmail.tsx
  - tests/unit/email.test.ts
  - tests/unit/overdue-scan.test.ts
autonomous: false
requirements: [NOTF-04]
user_setup:
  - service: resend
    why: "Transactional email delivery for due-date, overdue, and hold-ready notifications"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard -> API Keys -> Create API Key"
      - name: RESEND_FROM_EMAIL
        source: "Resend Dashboard -> Domains (verified domain required for prod; dev can use 'Library <onboarding@resend.dev>')"

must_haves:
  truths:
    - "A server-side call to sendAndLog sends an email via Resend and records a NotificationLog row"
    - "Every send attempt writes a NotificationLog row with success true on delivery and success false on Resend error"
    - "Three React Email templates render valid HTML from typed props without Date objects"
  artifacts:
    - path: "src/lib/email.ts"
      provides: "Resend client singleton + sendAndLog() helper that writes NotificationLog"
      contains: "sendAndLog"
    - path: "src/emails/DueDateReminderEmail.tsx"
      provides: "Due-date reminder template (3-day and same-day)"
      exports: ["DueDateReminderEmail"]
    - path: "src/emails/OverdueAlertEmail.tsx"
      provides: "Overdue alert template"
      exports: ["OverdueAlertEmail"]
    - path: "src/emails/HoldReadyEmail.tsx"
      provides: "Hold-ready pickup template"
      exports: ["HoldReadyEmail"]
    - path: "tests/unit/email.test.ts"
      provides: "NOTF-04 coverage — NotificationLog written on success and failure"
      contains: "sendAndLog"
    - path: "tests/unit/overdue-scan.test.ts"
      provides: "Wave 0 scaffold for NOTF-01/NOTF-02 (consumed by plan 02)"
      contains: "scanAndNotify"
  key_links:
    - from: "src/lib/email.ts"
      to: "prisma.notificationLog"
      via: "create after Resend call (outside any transaction)"
      pattern: "notificationLog\\.create"
    - from: "src/lib/email.ts"
      to: "resend.emails.send"
      via: "Resend SDK call with idempotencyKey"
      pattern: "emails\\.send"
---

<objective>
Establish the email-sending foundation: install Resend + React Email + node-cron dependencies, add the Resend client singleton with a `sendAndLog()` helper that records every attempt to NotificationLog (NOTF-04), and author the three React Email templates. This is the thinnest vertical slice that lets any server-side caller send a logged email end-to-end.

Purpose: All downstream slices (cron scan in plan 02, hold-ready in plan 03, delivery-log UI in plan 04) consume `sendAndLog` and the templates. Defining the email contract first prevents a scavenger hunt for the send signature.
Output: Working, tested email helper and templates; deps installed; env documented.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-notifications-backups/04-RESEARCH.md
@.planning/phases/04-notifications-backups/04-PATTERNS.md
@CLAUDE.md

# Existing patterns to mirror
@src/lib/db.ts
@prisma/schema.prisma
</context>

<artifacts_this_phase_produces>
- `src/lib/email.ts` — Resend singleton + `sendAndLog()` helper (the email contract for the whole phase)
- `src/emails/DueDateReminderEmail.tsx`, `OverdueAlertEmail.tsx`, `HoldReadyEmail.tsx` — React Email templates
- `tests/unit/email.test.ts` — NOTF-04 unit coverage
- `tests/unit/overdue-scan.test.ts` — Wave 0 scaffold consumed by plan 02
- New dependencies in `package.json`: `resend`, `react-email`, `@react-email/components`, `@react-email/render`, `node-cron`, `@types/node-cron`
- `.env.example` entries: `RESEND_API_KEY`, `RESEND_FROM_EMAIL`
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy verification + dependency install</name>
  <read_first>
    - .planning/phases/04-notifications-backups/04-RESEARCH.md (## Package Legitimacy Audit, ## Standard Stack — all packages Approved)
    - package.json (current dependency versions; confirm none already present)
  </read_first>
  <what-built>
    The five runtime packages and one devDependency for this phase are all marked Approved in RESEARCH.md's Package Legitimacy Audit (resend, react-email, @react-email/components, @react-email/render, node-cron all trace to official Resend / node-cron GitHub repos; no postinstall scripts). Per the package legitimacy gate, a human must confirm before installation proceeds. After approval the executor runs the install.
  </what-built>
  <how-to-verify>
    1. Confirm the Package Legitimacy Audit table in 04-RESEARCH.md lists all six packages with disposition "Approved" and "Packages flagged as suspicious [SUS]: none".
    2. Spot-check on the registries if desired: npmjs.com/package/resend, npmjs.com/package/node-cron, npmjs.com/package/@react-email/components.
    3. On approval, the executor runs: `npm install resend react-email @react-email/components @react-email/render node-cron` and `npm install --save-dev @types/node-cron`.
    4. Confirm `package.json` lists the new dependencies and `npm test` still runs (existing suite unaffected).
  </how-to-verify>
  <action>Present the RESEARCH.md Package Legitimacy Audit verdicts to the user. After explicit approval, install the five runtime packages (resend, react-email, @react-email/components, @react-email/render, node-cron) and the devDependency @types/node-cron. Do NOT pin to a registry-fetched "latest" beyond the RESEARCH.md-verified versions if a conflict arises; report and pause instead. This checkpoint is never auto-approvable.</action>
  <verify>
    <automated>node -e "const p=require('./package.json');const need=['resend','react-email','@react-email/components','@react-email/render','node-cron'];const miss=need.filter(n=>!p.dependencies[n]);if(miss.length){console.error('MISSING',miss);process.exit(1)};if(!p.devDependencies['@types/node-cron']){console.error('MISSING @types/node-cron');process.exit(1)};console.log('all deps present')"</automated>
  </verify>
  <acceptance_criteria>
    - All five runtime packages appear in `package.json` dependencies and `@types/node-cron` in devDependencies.
    - `node_modules/resend` and `node_modules/node-cron` exist.
    - Existing `npm test` suite still passes (no install-time breakage).
  </acceptance_criteria>
  <resume-signal>Type "approved" to install, or describe concerns.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Email send helper (sendAndLog) + .env.example + email.test.ts</name>
  <files>src/lib/email.ts, .env.example, tests/unit/email.test.ts, tests/unit/overdue-scan.test.ts</files>
  <read_first>
    - .planning/phases/04-notifications-backups/04-PATTERNS.md (### src/lib/email.ts section — singleton + sendAndLog pattern, NOTF-04 always-log rule)
    - .planning/phases/04-notifications-backups/04-RESEARCH.md (Pattern 2, Pitfall 2, Pitfall 5, Security Domain RESEND_API_KEY rule)
    - src/lib/db.ts (globalThis singleton pattern to mirror)
    - prisma/schema.prisma (model NotificationLog — fields: memberId, type, sentAt, channel default "EMAIL", success Boolean, metadata Json?; NO member relation, NO createdAt)
  </read_first>
  <behavior>
    - Test 1 (NOTF-04 success): when the mocked Resend client resolves with `{ data: { id: "x" }, error: null }`, `sendAndLog` returns `{ success: true }` AND `prisma.notificationLog.create` is called once with `success: true`, the given `memberId`, `type`, and `channel: "EMAIL"`.
    - Test 2 (NOTF-04 failure): when the mocked Resend client resolves with `{ data: null, error: { name: "rate_limited" } }`, `sendAndLog` returns `{ success: false }` AND `notificationLog.create` is called once with `success: false`.
    - Test 3 (NOTF-04 throw path): when the mocked Resend send throws, `sendAndLog` does not rethrow and still calls `notificationLog.create` with `success: false`.
    - Test 4 (idempotency wiring): the `idempotencyKey` argument passed to `sendAndLog` is forwarded as the second argument of `resend.emails.send`.
  </behavior>
  <action>Create `src/lib/email.ts`: instantiate `new Resend(process.env.RESEND_API_KEY)` guarded by a `globalForResend` globalThis singleton mirroring `src/lib/db.ts` (assign to global only when NODE_ENV !== "production"). Export `type NotificationType = "DUE_DATE_3DAY" | "DUE_DATE_SAME" | "OVERDUE_ALERT" | "HOLD_READY"`. Export `async function sendAndLog(opts: { to: string; subject: string; react: React.ReactElement; memberId: string; type: NotificationType; idempotencyKey: string; metadata?: Record<string, unknown> }): Promise<{ success: boolean }>`. Inside: call `resend.emails.send({ from: process.env.RESEND_FROM_EMAIL ?? "Library <onboarding@resend.dev>", to: [opts.to], subject: opts.subject, react: opts.react }, { idempotencyKey: opts.idempotencyKey })` wrapped in try/catch; set `success = !error && !!data?.id`, catch sets `success = false`. ALWAYS (success or failure, never inside a transaction) call `prisma.notificationLog.create({ data: { memberId: opts.memberId, type: opts.type, channel: "EMAIL", success, metadata: opts.metadata ?? null } })`. Return `{ success }`. Use `import { prisma } from "@/lib/db"`. Never use a NEXT_PUBLIC_ prefix for the key. Add `RESEND_API_KEY=` and `RESEND_FROM_EMAIL=Library <onboarding@resend.dev>` to `.env.example` with a comment that a verified domain is required for production. Create `tests/unit/email.test.ts` covering the four behaviors above by mocking the `resend` module and `@/lib/db`. Also create the Wave 0 scaffold `tests/unit/overdue-scan.test.ts` that imports `scanAndNotify` from `@/jobs/overdue-scan` and contains a placeholder test marked `it.todo` for NOTF-01 and NOTF-02 (plan 02 implements the real assertions) — this file must not fail the suite (use `describe.todo` or `it.todo` so the missing module does not error; if an import of a not-yet-existing module would break collection, gate the import inside the todo with a comment instructing plan 02 to wire it).</action>
  <verify>
    <automated>npm test -- tests/unit/email.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- tests/unit/email.test.ts` passes all four behavior tests.
    - `src/lib/email.ts` exports `sendAndLog` and `NotificationType`; grep `grep -v '^//' src/lib/email.ts | grep -c "notificationLog.create"` returns >= 1.
    - `src/lib/email.ts` contains no `NEXT_PUBLIC_` reference: `grep -c "NEXT_PUBLIC_" src/lib/email.ts` returns 0.
    - `.env.example` contains both `RESEND_API_KEY` and `RESEND_FROM_EMAIL`.
    - The full `npm test` suite still collects without import errors from the overdue-scan scaffold.
  </acceptance_criteria>
  <done>sendAndLog sends via Resend and always writes a NotificationLog row (success or failure); env documented; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Three React Email templates</name>
  <files>src/emails/DueDateReminderEmail.tsx, src/emails/OverdueAlertEmail.tsx, src/emails/HoldReadyEmail.tsx</files>
  <read_first>
    - .planning/phases/04-notifications-backups/04-PATTERNS.md (### src/emails/*.tsx section — template structure, prop-naming rule: pre-formatted date strings, never Date objects)
    - .planning/phases/04-notifications-backups/04-RESEARCH.md (Pattern 3 — DueDateReminderEmail example)
  </read_first>
  <action>Create three typed React Email components using `@react-email/components` primitives (Html, Head, Body, Container, Text, Preview; Button where a call-to-action helps). DueDateReminderEmail props: `{ memberName: string; bookTitle: string; dueDate: string; daysUntilDue: number }` — copy varies on `daysUntilDue === 0` (due today) vs >0 (due in N days). OverdueAlertEmail props: `{ memberName: string; bookTitle: string; daysOverdue?: number }` — urges return to stop accruing fines. HoldReadyEmail props: `{ memberName: string; bookTitle: string; pickupWindowHours: number }` — informs the reserved copy is ready and states the pickup window. Every date/number is passed pre-formatted as a string or primitive; NO `Date` objects in props (UTC epoch math is the caller's responsibility). Each file `export function <Name>(props) { ... }` and `import * as React from "react"`.</action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "src/emails" || echo "emails typecheck clean"</automated>
  </verify>
  <acceptance_criteria>
    - Three files exist under `src/emails/` each exporting a named function component.
    - `grep -l "@react-email/components" src/emails/*.tsx` lists all three files.
    - No `Date` typed prop: `grep -c ": Date" src/emails/*.tsx` returns 0 for each.
    - TypeScript compiles the email files without errors.
  </acceptance_criteria>
  <done>Three templates render from typed string/primitive props with no Date objects; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server -> Resend API | Outbound email; API key is a secret crossing process -> third party |
| email helper -> database | sendAndLog writes NotificationLog (append-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | RESEND_API_KEY in client bundle | mitigate | Key read only in `src/lib/email.ts` (server module); never `NEXT_PUBLIC_`; verified by grep gate in Task 2 acceptance |
| T-04-02 | Tampering | NotificationLog integrity | accept | Append-only model, no delete/update path; low-value internal record |
| T-04-03 | Denial of Service | Resend rate limits / failures | mitigate | sendAndLog never throws on send failure; logs success:false and continues |
| T-04-SC | Tampering | npm installs (resend, react-email, node-cron) | mitigate | Package Legitimacy Audit (all Approved, no postinstall) + blocking-human checkpoint in Task 1 |
</threat_model>

<verification>
- `npm test -- tests/unit/email.test.ts` green (NOTF-04).
- Full `npm test` suite collects and passes.
- `package.json` contains all six new packages.
- Grep gates: no NEXT_PUBLIC_ in email.ts; notificationLog.create present; no Date props in templates.
</verification>

<success_criteria>
- sendAndLog sends a Resend email and records a NotificationLog row on both success and failure (NOTF-04).
- Three email templates exist and typecheck.
- Dependencies installed after human legitimacy approval.
</success_criteria>

<output>
Create `.planning/phases/04-notifications-backups/04-01-SUMMARY.md` when done.
</output>

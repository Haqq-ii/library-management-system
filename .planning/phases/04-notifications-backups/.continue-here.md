---
context: phase
phase: 04-notifications-backups
task: 0
total_tasks: 1
status: in_progress
last_updated: 2026-06-22T13:31:29.689Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints identified this session._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Wave 1 file overlap | 04-01 and 04-05 both modify .env.example — must run sequentially | advisory | Check files_modified overlap before spawning parallel agents |

<current_state>
Phase 4 Wave 3 is the only remaining work. Plans 04-01, 04-05 (Wave 1) and 04-02, 04-04 (Wave 2) are all complete. 60/60 tests green. Wave 3 has one plan: 04-03 hold-ready slice.
</current_state>

<completed_work>

- 04-01 DONE: Resend + React Email + node-cron deps installed; `src/lib/email.ts` (sendAndLog singleton); three React Email templates (DueDateReminderEmail, OverdueAlertEmail, HoldReadyEmail); 4 unit tests green
- 04-05 DONE: `docker-compose.yml` db-backup sidecar (daily pg_dump at 02:00 UTC); README backup section; backups/ gitignored; human checkpoint verified
- 04-02 DONE: `output: "standalone"` removed from next.config.ts; `server.ts` custom server with node-cron daily 06:00 UTC job; `src/jobs/overdue-scan.ts` (scanAndNotify); `src/lib/notifications.ts` (sendDueDateReminder, sendOverdueAlert); 6 NOTF-01/02 tests green
- 04-04 DONE: `src/features/notifications/actions.ts` (getNotificationLog, requireRole LIBRARIAN); `NotificationLogTable.tsx`; `/notifications` page; AppSidebar Bell icon nav entry
- test-fix DONE: Updated stale Phase 3 mocks in loan-return, loan-actions, catalog-actions, member-actions tests — all 60 tests green (commit 0b98f92)
</completed_work>

<remaining_work>

- **04-03** (Wave 3): Hold-ready slice (NOTF-03) — add `sendHoldReady()` wrapper to `src/lib/notifications.ts`, then call it from `returnBook` in `src/features/loans/actions.ts` **after** the `prisma.$transaction` commits when `holdTriggered: true`. Must pass `holdMemberId`, `holdMemberEmail`, `bookTitle` from the transaction return shape. The `HoldReadyEmail` template and `sendAndLog` are already in place.
</remaining_work>

<decisions_made>

- Removed `output: "standalone"` from next.config.ts — instrumentation.ts is broken with standalone output (Next.js issue #89377); custom server.ts required for node-cron to run in production Docker
- Dockerfile updated to standard Node.js build (`tsx server.ts`) instead of standalone `node server.js`
- Wave 1 executed sequentially (not parallel) because 04-01 and 04-05 both modify `.env.example`
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `.planning/phases/04-notifications-backups/04-03-PLAN.md` — the only remaining plan
2. `src/lib/email.ts` — sendAndLog already implemented; sendHoldReady wraps it
3. `src/features/loans/actions.ts` — returnBook transaction return shape; need holdMemberId, holdMemberEmail, bookTitle in return value
4. `src/lib/notifications.ts` — sendDueDateReminder and sendOverdueAlert already here; add sendHoldReady alongside them

## Infrastructure State
- All deps installed: resend, react-email, @react-email/components, @react-email/render, node-cron, @types/node-cron
- RESEND_API_KEY not yet set in local .env (only documented in .env.example) — emails will fail at runtime but tests mock Resend

<context>
Session paused at 77% context after completing Waves 1 and 2 of Phase 4. Wave 3 is a single short plan (04-03, 1 task, autonomous). The key implementation point: sendHoldReady must be called AFTER the transaction (not inside it) to avoid holding the DB connection open during the Resend HTTP call. The returnBook transaction already returns holdTriggered + holdMemberName; it needs to also return holdMemberId + holdMemberEmail + bookTitle for the email call.
</context>

<next_action>
Run: `/gsd-execute-phase 4 --wave 3`
This executes only Wave 3 (plan 04-03). After it completes the orchestrator will run code review, regression gate, schema drift check, and verifier — completing Phase 4.
</next_action>

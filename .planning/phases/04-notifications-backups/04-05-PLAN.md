---
phase: 04-notifications-backups
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - .env.example
  - README.md
autonomous: false
requirements: [INFRA-05]

must_haves:
  truths:
    - "A self-hosted Docker deployment runs an automated daily pg_dump producing a dated backup file"
    - "A backup file can be verified to exist in the backups volume after the scheduled window"
    - "The Railway managed-backup configuration is documented for the production deployment path"
  artifacts:
    - path: "docker-compose.yml"
      provides: "db-backup sidecar service running daily pg_dump on a cron schedule"
      contains: "db-backup"
    - path: "README.md"
      provides: "Backup documentation: Railway native backups + self-hosted sidecar usage and verification"
      contains: "Backup"
  key_links:
    - from: "docker-compose.yml db-backup"
      to: "db service"
      via: "depends_on service_healthy + PGHOST"
      pattern: "service_healthy"
    - from: "docker-compose.yml db-backup"
      to: "./backups volume"
      via: "pg_dump output mount"
      pattern: "/backups"
---

<objective>
Deliver the database-backup vertical slice (INFRA-05): a `db-backup` sidecar in docker-compose.yml that runs a daily `pg_dump` to a mounted `./backups` directory for self-hosted Docker, plus README documentation of both the self-hosted sidecar and Railway's native managed-backup configuration. Verified by a human checkpoint that confirms a backup file is produced.

Purpose: INFRA-05 requires automated backups on a schedule with a verifiable artifact. Per RESEARCH, this is infrastructure-only — Railway is zero-code (UI config), self-hosted is a compose sidecar. No application code.
Output: docker-compose sidecar, .env.example/README docs, human verification that a dump file appears.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-notifications-backups/04-RESEARCH.md
@.planning/phases/04-notifications-backups/04-PATTERNS.md
@CLAUDE.md

# Self — file being modified
@docker-compose.yml
</context>

<artifacts_this_phase_produces>
- `docker-compose.yml` — `db-backup` sidecar service + `./backups` mount (and `backups_data` volume entry if a named volume is chosen)
- `README.md` — backup section (Railway native + self-hosted sidecar + verification steps)
- `.env.example` — optional `BACKUP_SCHEDULE` / `PGDATABASE` doc note (only if parameterized)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: pg_dump backup sidecar in docker-compose.yml + README docs</name>
  <files>docker-compose.yml, README.md, .env.example</files>
  <read_first>
    - .planning/phases/04-notifications-backups/04-PATTERNS.md (### docker-compose.yml MODIFIED section — existing db service structure to mirror + pg_dump sidecar addition)
    - .planning/phases/04-notifications-backups/04-RESEARCH.md (Code Examples: pg_dump sidecar for Docker self-hosted; Railway Backup Configuration; Security Domain: backup contains plaintext — store in volume, not git)
    - docker-compose.yml (current services: db with POSTGRES_DB=library_dev, user/password postgres; existing volumes section)
  </read_first>
  <action>Add a `db-backup` service to `docker-compose.yml` modeled on the existing `db` service: `image: postgres:16-alpine`; environment `PGHOST: db`, `PGUSER: postgres`, `PGPASSWORD: postgres`, `PGDATABASE: library_dev` (match the existing db service's credentials/db name exactly — read them from the current file, do not assume); `volumes: - ./backups:/backups` (host bind mount for easy access); `depends_on: db: { condition: service_healthy }`; `command:` a shell that writes an Alpine crontab running daily at 02:00 `pg_dump -Fc $$PGDATABASE > /backups/db-$$(date +%F).dump` then runs `crond -f -l 2` (use the verbatim command from PATTERNS.md, keeping the doubled `$$` so compose does not interpolate). Ensure `./backups` is gitignored (add `backups/` to `.gitignore` if not present — backups contain plaintext data and must never be committed; verify the existing .gitignore). Add a "## Database Backups" section to README.md documenting: (a) Railway production path — Dashboard -> PostgreSQL service -> Settings -> Backups -> enable Daily (retained 6 days) / optional Weekly, zero code (cite docs.railway.com/reference/backups); (b) self-hosted path — the db-backup sidecar runs daily pg_dump into ./backups; (c) verification — after the 02:00 window (or by manually invoking `docker compose exec db-backup sh -c "pg_dump -Fc library_dev > /backups/db-manual.dump"`), confirm a `db-YYYY-MM-DD.dump` file exists in `./backups`. Optionally note the schedule is editable in the sidecar command. Do not commit any actual .dump files.</action>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('docker-compose.yml','utf8');if(!/db-backup/.test(y)){console.error('no db-backup service');process.exit(1)};if(!/pg_dump/.test(y)){console.error('no pg_dump command');process.exit(1)};if(!/service_healthy/.test(y)){console.error('missing depends_on healthcheck');process.exit(1)};const r=require('fs').readFileSync('README.md','utf8');if(!/[Bb]ackup/.test(r)){console.error('README missing backup docs');process.exit(1)};console.log('backup config + docs ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `docker-compose.yml` contains a `db-backup` service with `image: postgres:16-alpine`, a `pg_dump` command, `./backups:/backups` mount, and `depends_on db service_healthy`.
    - Credentials/db name in the sidecar match the existing `db` service exactly.
    - `README.md` has a backup section covering Railway native backups AND the self-hosted sidecar with a verification step.
    - `backups/` is gitignored: `grep -c "backups" .gitignore` returns >= 1.
    - `docker compose config` parses without error (YAML valid).
  </acceptance_criteria>
  <done>Self-hosted daily pg_dump sidecar configured; backup paths documented; backups directory excluded from git.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify a backup file is produced</name>
  <read_first>
    - README.md (## Database Backups verification steps written in Task 1)
    - docker-compose.yml (db-backup service)
  </read_first>
  <what-built>
    A `db-backup` sidecar that runs `pg_dump` daily at 02:00 UTC into `./backups`, plus README docs for both the self-hosted sidecar and Railway native backups. INFRA-05 has no application code to unit-test, so its proof is the existence of a real backup file.
  </what-built>
  <how-to-verify>
    1. Bring the stack up: `docker compose up -d db db-backup` (and confirm `docker compose ps` shows db-backup running).
    2. Trigger a manual dump rather than waiting for 02:00: `docker compose exec db-backup sh -c "pg_dump -Fc library_dev > /backups/db-manual.dump"`.
    3. Confirm the file exists on the host: `ls -la ./backups/` should list a `.dump` file with non-zero size.
    4. (Production note, no action required now) Confirm the README documents enabling Daily backups in the Railway dashboard for the deployed environment.
  </how-to-verify>
  <action>Pause for the user to run the verification steps. The executor has already configured the sidecar and docs; this checkpoint confirms a backup artifact is actually produced (INFRA-05 success criterion: "a backup can be verified to exist after the scheduled window").</action>
  <verify>
    <human-check>A non-zero `.dump` file exists in `./backups/` after running the manual pg_dump (or after the scheduled window).</human-check>
  </verify>
  <acceptance_criteria>
    - User confirms a `.dump` file with non-zero size exists in `./backups/`.
    - User confirms the Railway backup documentation in README is accurate for their deployment target.
  </acceptance_criteria>
  <resume-signal>Type "verified" once a backup file exists, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| db-backup sidecar -> backups volume | full database contents (PII, password hashes) written to disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | Information Disclosure | backup file with plaintext data committed to git | mitigate | `backups/` added to .gitignore; verified by grep gate in Task 1 |
| T-04-15 | Information Disclosure | backup contains password hashes | accept | Stored in isolated Docker volume / Railway per-service backup; not exposed over network; standard for DB backups |
| T-04-16 | Denial of Service | unbounded backup growth fills disk | accept | Single-institution scale; retention pruning deferred to v2 ops; documented in README as an operator concern |
</threat_model>

<verification>
- `docker compose config` parses; db-backup service present with pg_dump + healthcheck dependency.
- README backup section present; backups/ gitignored.
- Human checkpoint: a non-zero .dump file exists in ./backups.
</verification>

<success_criteria>
- Automated daily pg_dump backup configured for self-hosted Docker (INFRA-05).
- Railway native backup path documented for production.
- A backup file verified to exist after the scheduled window (human-confirmed).
</success_criteria>

<output>
Create `.planning/phases/04-notifications-backups/04-05-SUMMARY.md` when done.
</output>

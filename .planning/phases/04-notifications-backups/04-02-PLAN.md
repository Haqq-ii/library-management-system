---
phase: 04-notifications-backups
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - next.config.ts
  - Dockerfile
  - package.json
  - server.ts
  - src/lib/notifications.ts
  - src/jobs/overdue-scan.ts
  - tests/unit/overdue-scan.test.ts
autonomous: true
requirements: [NOTF-01, NOTF-02]

must_haves:
  truths:
    - "A daily scheduled job runs automatically inside the server process without librarian action"
    - "Loans due in 0-3 days trigger a due-date reminder email (same-day vs 3-day variant)"
    - "Loans past due with no return date trigger an overdue alert email each daily run"
    - "Loans due more than 3 days out trigger no email; returned loans trigger no email"
  artifacts:
    - path: "server.ts"
      provides: "Custom Next.js server that registers the daily node-cron job"
      contains: "cron.schedule"
    - path: "src/jobs/overdue-scan.ts"
      provides: "scanAndNotify() — queries loans and dispatches due/overdue emails"
      exports: ["scanAndNotify"]
    - path: "src/lib/notifications.ts"
      provides: "sendDueDateReminder() and sendOverdueAlert() wrappers over sendAndLog"
      exports: ["sendDueDateReminder", "sendOverdueAlert"]
    - path: "next.config.ts"
      provides: "config WITHOUT output:standalone (custom server compatibility)"
  key_links:
    - from: "server.ts"
      to: "scanAndNotify"
      via: "cron.schedule callback"
      pattern: "scanAndNotify"
    - from: "src/jobs/overdue-scan.ts"
      to: "prisma.loan.findMany"
      via: "due-window and overdue queries"
      pattern: "loan\\.findMany"
    - from: "src/jobs/overdue-scan.ts"
      to: "sendAndLog"
      via: "via sendDueDateReminder / sendOverdueAlert"
      pattern: "send(DueDateReminder|OverdueAlert)"
---

<objective>
Deliver the scheduled-notification vertical slice: a custom `server.ts` that boots Next.js and registers a daily node-cron job, plus the `scanAndNotify()` handler that queries loans and sends due-date reminders (NOTF-01) and overdue alerts (NOTF-02) via the plan-01 email helper. Resolve the `output: "standalone"` conflict so the cron actually runs in production Docker.

Purpose: Without this slice, no email is ever sent automatically. The custom server is the only place node-cron survives in production (instrumentation.ts is broken with standalone output — RESEARCH Pitfall 1).
Output: Running cron job, overdue-scan handler, notification wrappers, updated build/Docker config, real NOTF-01/NOTF-02 tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-notifications-backups/04-RESEARCH.md
@.planning/phases/04-notifications-backups/04-PATTERNS.md
@CLAUDE.md

# Consumes plan 01 artifacts
@src/lib/email.ts
@src/emails/DueDateReminderEmail.tsx
@src/emails/OverdueAlertEmail.tsx

# Patterns to mirror
@src/features/loans/actions.ts
@next.config.ts
@Dockerfile
</context>

<artifacts_this_phase_produces>
- `server.ts` — custom server entry registering the 06:00 UTC daily cron
- `src/jobs/overdue-scan.ts` — `scanAndNotify()` handler
- `src/lib/notifications.ts` — `sendDueDateReminder()`, `sendOverdueAlert()` wrappers
- `next.config.ts` — standalone output removed
- `Dockerfile` — runner stage rebuilt to bundle and run `server.ts`
- `package.json` — `start` script updated to run the custom server
- `tests/unit/overdue-scan.test.ts` — real NOTF-01 / NOTF-02 assertions
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Remove standalone output + custom server.ts + Dockerfile runner</name>
  <files>next.config.ts, server.ts, Dockerfile, package.json</files>
  <read_first>
    - .planning/phases/04-notifications-backups/04-PATTERNS.md (### server.ts section — verbatim custom server pattern AND Dockerfile runner-stage replacement)
    - .planning/phases/04-notifications-backups/04-RESEARCH.md (Pattern 1, Pitfall 1, Open Questions #1 — recommendation: drop standalone, copy full node_modules)
    - next.config.ts (currently exports `{ output: "standalone" }`)
    - Dockerfile (current runner stage copies `.next/standalone` and runs `node server.js`)
    - package.json (scripts.start currently `next start`)
  </read_first>
  <action>BLOCKING first step: remove the `output: "standalone"` line from `next.config.ts`, leaving a valid (possibly empty) NextConfig export. Create `server.ts` at the PROJECT ROOT (not in src/) using the verbatim pattern from PATTERNS.md: import `createServer` from "http", `next`, `cron` from "node-cron", and `scanAndNotify` from "./src/jobs/overdue-scan"; read PORT from `process.env.PORT ?? "3000"`; `dev = process.env.NODE_ENV !== "production"`; in `app.prepare().then(...)` register `cron.schedule("0 6 * * *", async () => { console.log("[cron] Running daily overdue scan..."); await scanAndNotify(); }, { timezone: "UTC" })`, then start the HTTP server with the Next request handler. Rebuild the Dockerfile builder + runner stages per PATTERNS.md: builder runs `npm run build` then bundles the server with tsx/esbuild (`npx tsx --tsconfig tsconfig.json server.ts --outfile dist/server.js` or an esbuild equivalent that emits CJS to `dist/server.js`); runner copies `.next`, `public`, full `node_modules`, and `dist/server.js` as `./server.js`, then `CMD ["node", "server.js"]`. Update `package.json` scripts: set `"start": "NODE_ENV=production tsx server.ts"` (or `node dist/server.js` if a prebuild step bundles it) and add a `"start:dev": "tsx server.ts"` for local custom-server runs. Do not break the existing `dev` script.</action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('next.config.ts','utf8');if(/output:\s*['\"]standalone['\"]/.test(s)){console.error('standalone still present');process.exit(1)};const sv=require('fs').readFileSync('server.ts','utf8');if(!/cron\.schedule/.test(sv)||!/scanAndNotify/.test(sv)){console.error('server.ts missing cron wiring');process.exit(1)};console.log('config + server ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "standalone" next.config.ts` returns 0.
    - `server.ts` exists at project root, references `cron.schedule` and `scanAndNotify`.
    - Dockerfile runner stage no longer references `.next/standalone`; it copies `node_modules` and runs the custom server.
    - `package.json` start script runs the custom server (not bare `next start`).
  </acceptance_criteria>
  <done>Standalone output removed, custom server registers the daily cron, Docker/build config runs the custom server in production.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Notification wrappers + overdue-scan handler + NOTF-01/02 tests</name>
  <files>src/lib/notifications.ts, src/jobs/overdue-scan.ts, tests/unit/overdue-scan.test.ts</files>
  <read_first>
    - .planning/phases/04-notifications-backups/04-PATTERNS.md (### src/jobs/overdue-scan.ts — verbatim handler; ### Shared Patterns — UTC epoch math, Prisma singleton import)
    - .planning/phases/04-notifications-backups/04-RESEARCH.md (Pattern 4, Pattern 7 idempotency format, Pitfall 3, Pitfall 4 — filter dueAt<now AND returnedAt IS NULL, not status only)
    - src/lib/email.ts (sendAndLog signature + NotificationType from plan 01)
    - src/emails/DueDateReminderEmail.tsx, src/emails/OverdueAlertEmail.tsx (template prop shapes)
    - src/features/loans/actions.ts (lines ~163-170 Prisma include shape; lines ~184-186 UTC epoch math)
  </read_first>
  <behavior>
    - Test 1 (NOTF-01 due-window): given a loan with status ACTIVE, returnedAt null, dueAt 2 days from now, scanAndNotify calls the due-date path (sendAndLog/sendDueDateReminder) once with type "DUE_DATE_3DAY".
    - Test 2 (NOTF-01 same-day): a loan dueAt later today resolves to type "DUE_DATE_SAME".
    - Test 3 (NOTF-01 negative): a loan dueAt 10 days from now triggers NO due-date send.
    - Test 4 (NOTF-02 overdue): a loan with returnedAt null and dueAt in the past triggers an overdue send (type "OVERDUE_ALERT"), regardless of its status field value (set status:"ACTIVE" to prove the dueAt<now filter, not status==OVERDUE).
    - Test 5 (NOTF-02 negative): a loan with returnedAt set (already returned) but dueAt in the past triggers NO overdue send.
    - Test 6 (idempotency key format): overdue send uses key `OVERDUE_ALERT/<loanId>/<YYYY-MM-DD>`; due send uses `<type>/<loanId>/<YYYY-MM-DD>`.
  </behavior>
  <action>Create `src/lib/notifications.ts` exporting `async function sendDueDateReminder(opts: { loan; daysUntilDue: number; idempotencyKey: string; type: "DUE_DATE_3DAY" | "DUE_DATE_SAME" })` and `async function sendOverdueAlert(opts: { loan; idempotencyKey: string; daysOverdue?: number })`. Each builds the React element via `React.createElement(<Template>, {...})` from member/book fields (`loan.member.user.name`, `loan.member.user.email`, `loan.copy.book.title`) and delegates to `sendAndLog` from `@/lib/email` with the correct `to`, `subject`, `memberId: loan.memberId`, `type`, and `idempotencyKey`. Pre-format `dueDate` as `new Date(loan.dueAt).toISOString().slice(0,10)`. Create `src/jobs/overdue-scan.ts` exporting `async function scanAndNotify(): Promise<void>` per PATTERNS.md verbatim: compute `now`, `todayUTC = now.toISOString().slice(0,10)`, `in3Days = now + 3 days`; query upcoming loans `where: { status: "ACTIVE", dueAt: { lte: in3Days, gte: now }, returnedAt: null }` with member.user and copy.book includes; for each compute `daysUntilDue` via getTime() rounding, derive type (0 => DUE_DATE_SAME else DUE_DATE_3DAY), build idempotencyKey `${type}/${loan.id}/${todayUTC}`, call sendDueDateReminder. Then query overdue loans `where: { returnedAt: null, dueAt: { lt: now } }` (NOT status-based — Pitfall 4) with same includes; for each build key `OVERDUE_ALERT/${loan.id}/${todayUTC}` and call sendOverdueAlert. Wrap the body in try/catch logging `console.error("[scanAndNotify]", err)` so one bad loan does not abort the run. Replace the plan-01 scaffold `tests/unit/overdue-scan.test.ts` todos with the six real behaviors above by mocking `@/lib/db` (prisma.loan.findMany returning controlled fixtures) and `@/lib/email` (spy on sendAndLog) — assert call counts and idempotency key strings.</action>
  <verify>
    <automated>npm test -- tests/unit/overdue-scan.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- tests/unit/overdue-scan.test.ts` passes all six behaviors.
    - `grep -c "status: \"OVERDUE\"" src/jobs/overdue-scan.ts` returns 0 (overdue query filters by dueAt<now, not status).
    - `src/jobs/overdue-scan.ts` exports `scanAndNotify`; `src/lib/notifications.ts` exports both wrappers.
    - Idempotency keys match the `<TYPE>/<loanId>/<YYYY-MM-DD>` format (asserted in tests).
  </acceptance_criteria>
  <done>Cron handler queries loans and dispatches due-date + overdue emails with date-scoped idempotency keys; NOTF-01/02 tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron process -> database | scanAndNotify reads loan/member data server-side |
| cron process -> Resend API | outbound email dispatch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Denial of Service | duplicate emails on cron drift / container restart | mitigate | Date-scoped Resend idempotency key `<TYPE>/<loanId>/<YYYY-MM-DD>`; Resend dedupes within 24h |
| T-04-05 | Denial of Service | one malformed loan aborts whole scan | mitigate | try/catch around scan body logs and continues |
| T-04-06 | Information Disclosure | cron exposed as HTTP endpoint | accept | Cron is in-process (server.ts), not a Route Handler; no external trigger surface |
| T-04-07 | Tampering | overdue loans missed due to status-field reliance | mitigate | Query `dueAt < now AND returnedAt IS NULL`, not `status == OVERDUE` (Pitfall 4) |
</threat_model>

<verification>
- `npm test -- tests/unit/overdue-scan.test.ts` green (NOTF-01, NOTF-02).
- `next.config.ts` has no standalone output; `server.ts` registers cron; Dockerfile runs custom server.
- Full `npm test` suite green.
</verification>

<success_criteria>
- Daily cron job runs scanAndNotify automatically in the server process (NOTF-01, NOTF-02).
- Due-in-3-days and same-day reminders sent; overdue alerts sent daily; far-future and returned loans excluded.
- Production Docker runs the custom server (standalone conflict resolved).
</success_criteria>

<output>
Create `.planning/phases/04-notifications-backups/04-02-SUMMARY.md` when done.
</output>

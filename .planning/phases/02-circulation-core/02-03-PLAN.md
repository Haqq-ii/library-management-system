---
phase: 02-circulation-core
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(app)/my-loans/page.tsx
  - tests/unit/my-loans-sections.test.ts
autonomous: true
requirements: [CIRC-04]
must_haves:
  truths:
    - "A member visiting /my-loans sees two distinct sections: Active Loans (top) and Loan History (below)"
    - "The Active Loans section lists only ACTIVE/OVERDUE loans; overdue rows are highlighted in red"
    - "The Loan History section lists all returned loans in reverse-chronological order"
    - "Each loan shows book title, author, due date, and a status badge (Active / Overdue / Returned)"
  artifacts:
    - path: "src/app/(app)/my-loans/page.tsx"
      provides: "Two-section member loans view (active + history)"
    - path: "tests/unit/my-loans-sections.test.ts"
      provides: "Unit test for the active/history partition + overdue helper"
  key_links:
    - from: "src/app/(app)/my-loans/page.tsx"
      to: "prisma.member"
      via: "findUnique with loans include"
      pattern: "prisma\\.member\\.findUnique"
---

<objective>
Update the member-facing `/my-loans` page from its flat single-table layout into two sections: Active Loans (current/overdue, highlighted) at the top and Loan History (returned, reverse-chronological) below. No tabs — a single scrollable page per D-12.

Purpose: CIRC-04 (member views their active loans and due dates) plus the loan-history view. This is the member half of Phase 2's "see who has what" value.

Output: rewritten `src/app/(app)/my-loans/page.tsx` with active/history partitioning, plus a small unit test for the partition + overdue logic extracted into a testable helper.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-circulation-core/02-CONTEXT.md
@.planning/phases/02-circulation-core/02-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this plan produces

**Route pages (modified):**
- `/my-loans` two-section layout — `src/app/(app)/my-loans/page.tsx`

**Utility functions:**
- `partitionLoans(loans)` → `{ active, history }` and `isLoanOverdue(loan)` helper (small exported pure functions co-located or in `src/features/loans/loan-display.ts`) — used by the page and unit-tested

**Tests:**
- `tests/unit/my-loans-sections.test.ts`
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite /my-loans into Active + History sections with a tested partition helper</name>
  <files>src/app/(app)/my-loans/page.tsx, src/features/loans/loan-display.ts, tests/unit/my-loans-sections.test.ts</files>
  <read_first>
    - src/app/(app)/my-loans/page.tsx (FULL current file: session check lines 16-17, member lookup with loans include lines 19-27, not-found guard lines 29-38, isOverdue + Badge variants lines 70-99, red date colouring lines 82-86)
    - .planning/phases/02-circulation-core/02-CONTEXT.md (D-12 two-section layout: Active Loans top with overdue highlight, Loan History below reverse-chronological, no tabs)
    - .planning/phases/02-circulation-core/02-PATTERNS.md (my-loans UPDATE section lines 66-95: partition into activeLoans/historyLoans, overdue highlight pattern, row bg-red-50)
    - prisma/schema.prisma (Loan: status enum ACTIVE/OVERDUE/RETURNED/LOST, returnedAt, dueAt, issuedAt)
    - tests/unit/member-actions.test.ts (vitest style; tests must be .test.ts under tests/unit per vitest.config include)
  </read_first>
  <action>
    Extract two pure helpers into `src/features/loans/loan-display.ts` (no `"use server"` — plain module, importable by both the page and the test):
    - `isLoanOverdue(loan: { status: string; dueAt: Date | string }): boolean` = `loan.status === "OVERDUE" || (loan.status === "ACTIVE" && new Date(loan.dueAt) < new Date())` (preserve the existing semantics from the current page lines 70-72).
    - `partitionLoans<T extends { status: string; returnedAt: Date | string | null }>(loans: T[]): { active: T[]; history: T[] }` where `active = loans.filter(l => l.status === "ACTIVE" || l.status === "OVERDUE")` and `history = loans.filter(l => l.returnedAt !== null)`.

    Rewrite `src/app/(app)/my-loans/page.tsx` keeping the existing session check, member lookup (with the same loans include), and not-found guard. Then:
    - Call `partitionLoans(member.loans)` to get `active` and `history`. The member query already orders `issuedAt: "desc"`, which gives reverse-chronological history; keep that ordering for history, and sort `active` by `dueAt` ascending (soonest due first) for parity with the librarian Active tab.
    - Render two `<section>` blocks: **Active Loans** (heading + count) on top and **Loan History** (heading + count) below — single scrollable page, no Tabs (D-12).
    - Active Loans table: reuse the existing column set (Book, Author, Issued, Due, Status). Apply `className={isLoanOverdue(loan) ? "bg-red-50" : undefined}` to the `<TableRow>` (extends the existing red date colouring) and keep the Badge variants (Active green / Overdue destructive). Empty state: "No active loans".
    - Loan History table: same columns; every row shows the outline "Returned" badge. Empty state: "No past loans".

    Create `tests/unit/my-loans-sections.test.ts` verifying: (a) `partitionLoans` puts ACTIVE and OVERDUE loans in `active` and returned loans in `history`; (b) a loan that is both ACTIVE and returned-null with a past dueAt is flagged by `isLoanOverdue`; (c) an ACTIVE loan with a future dueAt is not overdue. File must be `.test.ts`.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/my-loans-sections.test.ts && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `src/features/loans/loan-display.ts` exports `isLoanOverdue` and `partitionLoans` (no `"use server"` directive — grep confirms absence).
    - `my-loans/page.tsx` imports and uses `partitionLoans` and `isLoanOverdue`; renders separate "Active Loans" and "Loan History" headings (grep finds both strings).
    - Overdue active rows receive `bg-red-50` via `isLoanOverdue` (grep finds `bg-red-50`).
    - No Tabs import in `my-loans/page.tsx` (single scrollable page per D-12).
    - `tests/unit/my-loans-sections.test.ts` passes and `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Members see their active loans (overdue highlighted) and full returned history in two clearly separated sections.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /my-loans (Server Component) | A member requests their own loan data; must not see other members' loans |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Information Disclosure | /my-loans page | mitigate | Query scopes to `prisma.member.findUnique({ where: { userId: session.user.id } })` — a member only ever loads loans tied to their own session user id; no member-id param accepted from the client |
| T-02-12 | Elevation of Privilege | /my-loans page | mitigate | `auth.api.getSession` + `redirect("/login")` for unauthenticated requests (existing guard preserved) |
| T-02-13 | Tampering | partition/overdue helpers | accept | Pure display helpers with no writes and no external input beyond already-authorized loan rows — read-only, no mitigation required |
</threat_model>

<verification>
- `npx vitest run tests/unit/my-loans-sections.test.ts` passes (partition + overdue logic).
- `npx tsc --noEmit` passes.
- Manual/e2e (phase review): a member with active and returned loans sees two sections; an overdue active loan row is red.
</verification>

<success_criteria>
- CIRC-04: Member views their active loans with due dates; overdue loans are highlighted; full returned history is visible below.
- No cross-member data exposure (query scoped to session user).
</success_criteria>

<output>
Create `.planning/phases/02-circulation-core/02-03-SUMMARY.md` when done.
</output>
